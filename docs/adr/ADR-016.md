# ADR-016: RayNu-F — be the guest firmware for E5

- **Status:** Accepted
- **Date:** 2026-09-04
- **Pillars:** [Z] single binary · [A] audit ([V] architecture preserved — no Proven Core expansion)
- **Parent:** [ADR-009](ADR-009.md) (Mount Everest E5) · [ADR-014](ADR-014.md) (typed ISO / UEFI+virtio product boot) · supersedes the OVMF-relaunch **mechanism** used through Stage 46
- **Does not change:** E4 SHELL VMCS/EPT, `iso=0` path, Proven Core boundary (ADR-002), size budget (ADR-003)

## Context

Everest E5 needs a typed ISO to boot a UEFI installer to virtio-blk on real
R640 iron (`RAYNU-V-M7-ISO-INSTALL-OK`). The Stage 46 mechanism was: retain
the platform `OVMF.fd`, build a private VMCS + alias EPT, `VMLAUNCH` it at the
reset vector, and hand-emulate a whole PC (fw_cfg, ACPI/MTRR/GCD, PCI,
IDE/ATAPI, virtio, xAPIC/IOAPIC/PIC/PIT/HPET, 8042) until OVMF boots the ISO.

That mechanism reached, on iron: VMLAUNCH, past-SEC, DXE, CDROM-OK, VIRTIO-OK,
BOTH-OK, ATAPI-OK (`bf696ca`) and El Torito ELTORITO-OK (`0be7283`). But the
full product ISO (Stage 46) stalls **before** ATA (`ataio=0`): DxeCore parks in
`CoreWaitForEvent` on a timer event that never signals.

The ladder from `2d4ab51` → `4e16b59` (retaddr → ConIn → callsite → WFE
force-return → ZeroMem → preempt-skip → state4-poke → event-#PF inject) shared
one root defect: **we mutated OVMF's own internal control state to force
progress.** `CoreWaitForEvent(N, Events, OUT *Index)` must write `*Index`; the
force-return (`e0d5c55`) unwound the frame without it. Downstream, BDS then
walked a NULL `EFI_EVENT` (`r14=0` → `cmp [r14-0x48],'evnt'`) and page-faulted
(`#PF cr2=0xffffffffffffffb8`). The next five pins chased that fault we
created; `4e16b59` injected the `#PF`, OVMF's own handler ran, dumped a real
`#PF`, and `CpuDeadLoop`ed. Each pin cost a physical Cruzer flash. Nested QEMU
reaches ATAPI-OK where iron does not — the divergence is timer/interrupt
delivery behind our xAPIC/IOAPIC trap, not the boot logic.

Two lessons:

1. **Reaching into another firmware's private state to force it forward is a
   self-inflicted-wound generator.** We do not own OVMF's invariants; forcing
   one (a return without its OUT param) violates the next twenty.
2. We are reimplementing a device model by trial-and-error, one physical boot
   per bit of information, to satisfy a third-party firmware whose contract we
   only partially meet.

## Decision

1. **RayNu-F: be the guest firmware ourselves.** For the E5 product path, RayNu
   presents a **minimal, correct UEFI boot environment** to the ISO's own
   bootloader (`\EFI\BOOT\BOOTX64.EFI` — GRUB / systemd-boot / the Linux EFI
   stub) instead of relaunching and puppeting OVMF. We own the contract:
   an `EFI_SYSTEM_TABLE` + boot services with `BlockIo` / `SimpleFileSystem`
   over the CD (El Torito, already reached on iron) and virtio-blk, `LoadImage`
   / `StartImage`, serial `ConIn`/`ConOut`, `GetMemoryMap`/`ExitBootServices`,
   and a timer tick we deliver on our own schedule. Because we author every
   structure, **there is no third-party firmware internal state to corrupt.**

2. **RayNu-F is a subsystem, not a second binary.** It compiles into the single
   `r640-hypervisor.efi` ([Z] preserved). It runs inside the guest VMCS/EPT we
   already build. Naming: RayNu-**V** = the hypervisor; RayNu-**F** = the guest
   boot firmware it presents to a typed ISO.

3. **RayNu-F is OUTSIDE the Proven Core** (ADR-002). It is guest firmware, like
   the Linux boot protocol and device emulation. No LOC-budget change, no new
   Verus obligation. Standard Rust + tests (two-file convention).

4. **No third-party firmware state mutation (governance).** Neither RayNu-F nor
   the residual OVMF path may force or rewrite another firmware's internal
   control state (stack frames, event/object structures, gState, forced
   returns, injected faults to steer control flow) to manufacture progress.
   Drive guests only through **architected inputs**: interrupts, device
   register/DMA responses, memory the spec says is ours, and documented ABI
   returns. See the honesty constraint added to `CLAUDE.md`.

5. **Disable the Stage 46 forcing.** The `3k–3o` mutations
   (`guest_uefi_waitforevent_force_return` / `apply_waitforevent_return`,
   `try_firmware_wfe_state4_poke`, `deliver_firmware_wfe_event_pf`) are gated
   off (`RAYNU_F_NO_FW_STATE_MUTATION`). The retained-OVMF VMLAUNCH stays as a
   diagnostic/fallback, but it no longer mutates firmware internals. Refuse
   `4e16b59` / `--run 33820727776` (event-#PF inject → real `#PF` + CpuDeadLoop).

6. **Do not claim `RAYNU-V-M7-ISO-INSTALL-OK`. HDA `last_commit` stays
   `2b795a0`.** RayNu-F lands as honest scaffolding first (no EFI system table,
   no boot services, no ISO boot); each capability closes on its own evidence.

7. **ADR-014 typed-ISO contract stands.** RayNu-F *is* the "UEFI guest firmware"
   the ADR-014 product path assumed; `linux_iso` / `windows_iso` /
   `generic_uefi` and the boot-order spec are unchanged. `windows_iso` stays in
   the model.

## Consequences

- Iron flash discipline changes: the fast iteration harness is **nested / QEMU**
  (which already reaches ATAPI-OK); iron flashes are reserved for confirming the
  genuinely iron-specific parts (MTRR/GCD/phys-width, real-silicon LAPIC/timer
  delivery). Stop spending one physical boot per micro-experiment.
- RayNu-F must still deliver a periodic timer tick to the guest bootloader/kernel;
  the interrupt-delivery understanding the OVMF path lacked is now **our own**
  timer arch responsibility, not a puppeting hack.
- The retained-OVMF relaunch path stays compiled (past-SEC/DXE/ATAPI/El Torito
  evidence is real) but is a diagnostic/fallback, not the E5 close mechanism.
- Proven Core, ADR-002, ADR-003 size budget unchanged. RayNu-F guest firmware
  follows ADR-003 (lazy/zstd; 20 MB hard limit) if it grows embedded assets.
- Audit events for RayNu-F boot-service milestones live in the management/guest
  plane ([A]).

## RayNu-F stages (lived, tick only on evidence)

| # | Name | Track | Close when |
|---|------|-------|------------|
| **F0** | Scaffold | host | **CLOSED** — `RAYNU-V-RAYNU-F-SCAFFOLD-OK`: subsystem declared, outside Proven Core, honesty invariants (not functional / not ISO / never puppets foreign firmware). |
| **F1** | System table + serial console | host | **CLOSED (host)** — `RAYNU-V-RAYNU-F-TABLES-OK`: byte-exact UEFI 2.10 x64 `EFI_SYSTEM_TABLE` (120 B) + `EFI_BOOT_SERVICES` (44 fn) + `EFI_RUNTIME_SERVICES` (14 fn) + `SIMPLE_TEXT_OUTPUT/INPUT`, valid header CRC32s, `L"RayNu-F"` vendor; every fn pointer is a 14-byte guest trampoline (`mov r10,rdx; mov eax,id; mov dx,0x5246; out dx,eax; ret`) so a call is an ordinary I/O exit (no VMCALL, outside Proven Core); host dispatcher implements `ConOut.OutputString/Reset/TestString` + no-op cursor/attribute, `ConIn.ReadKeyStroke` → `EFI_NOT_READY`, boot/runtime → `EFI_UNSUPPORTED` (honest). Wired into `emulate_io_port`. **No guest has executed these tables yet.** |
| **F2a** | PE32+ loader + test app + launch plan | host | **CLOSED (host)** — `RAYNU-V-RAYNU-F-LOADER-OK`: PE32+ parser/loader (COFF, PE32+ optional header, sections, zero-fill, `.reloc` `DIR64`; refuses PE32/i386/oversize/unsupported reloc types); a RayNu-F-owned **test app** emitted as a genuine PE32+ (hand-encoded: `mov rax,[rdx+0x40]` → `ConOut`, `call [rax+8]` → `OutputString(L"RN-F ConOut via RayNu-F tables")`, then `hlt`) whose message pointer is fixed up through a real `DIR64` entry, so loading at the F2 base (`0x900000` ≠ `ImageBase 0x400000`) proves relocation end-to-end; a host-computed **launch plan** (tables `0x800000`, app `0x900000`, 64 KiB stack, identity PML4 `0x400000`, `RSP%16==8`, `CR0=0x80010033`, `CR4=0x620`, `EFER=0x500`, `RCX=ImageHandle`, `RDX=*SystemTable`). Nothing launched. |
| **F2b** | Guest launch | QEMU | **CLOSED (nested VT-x, `raynuvsrv1`, 2026-09-04).** EFI from run `33826787787` (`c975ade`), `RAYNU_F=1 REQUIRE_VMX=1 QEMU_ACCEL=kvm ./tools/qemu-boot-test.sh`: `RayNu-F VMLAUNCH entry=0x901000 system_table=0x801000 relocs=1` → guest printed `RN-F ConOut via RayNu-F tables` through **our** trampoline/dispatcher (`svc=ConOut.OutputString id=0x101 status=0x0`) → **`RAYNU-V-RAYNU-F-CONOUT-OK`** → `guest HLT rip=0x901019` (= entry+0x19, the app's `hlt`) → `stop hlt exits=2 svc=1 conout_ok=1`. Exactly two exits. First guest execution of RayNu-F-authored firmware tables. Not iron; iron confirmation is optional (`flashcruzer.sh --raynu-f`). Mechanism was: After the OVMF leg stops, with the slab/EPT/VMCS still owned and **no new allocations**: `VMCLEAR`, rebuild identity PTs at `0x400000`, write tables + relocated test app + zeroed stack + 3-entry GDT into the slab, overlay long-mode guest state (CR0/CR3/CR4/EFER, IA-32e entry ctl, 64-bit CS, flat data, exception bitmap all-ones), preload `RCX/RDX`, naked `VMLAUNCH`. Dedicated `raynu_f_vmexit` fast path (I/O `0x5246` → dispatcher, COM → UART, HLT → clean stop, any exception/EPT/triple-fault → dump + stop, exit cap). Opt-in via ESP `\EFI\RayNu\raynuf.txt` (`RAYNU_F=1` in the QEMU harness/CI, observational — gate does not require it). Closes only when a serial log shows `RAYNU-V-RAYNU-F-CONOUT-OK`, produced **only** by a live guest VM-exit, never by host/CI unit tests. |
| **F3** | Memory services + owned timer tick | QEMU | **CLOSED (nested `raynuvsrv1`, 2026-09-04: `TIMER-OK` + `MEM-OK` + `HLT … path=OK`, exits=7 svc_err=0).** `RAYNU-V-RAYNU-F-SERVICES-OK`: page allocator over a 20 MiB slab pool (`AllocatePages` Any/MaxAddress/Address, `FreePages`, page-granular `AllocatePool`/`FreePool` with a magic header), coalesced `GetMemoryMap` (40-byte v1 descriptors, `EFI_BUFFER_TOO_SMALL` sizing, contiguous cover of `[0, slab)`), `ExitBootServices` with map-key validation. **Owned timer:** a host-side firmware clock in 100 ns units — TSC calibrated pre-EBS against the platform's `Stall` — with `CreateEvent`/`CreateEventEx`, `SetTimer` (relative + drift-free periodic), `CheckEvent`, `WaitForEvent` (blocks on our clock, writes `*Index`, `EFI_TIMEOUT` rather than hanging when nothing can signal), `SignalEvent`, `CloseEvent`, TPL enforcement (`WaitForEvent` only at `TPL_APPLICATION`), `Stall`, `GetNextMonotonicCount`, `SetWatchdogTimer`, `CalculateCrc32`, `CopyMem` (memmove semantics), `SetMem`. `ConIn->WaitForKey` is a real tagged event fed by host serial RX; `ReadKeyStroke` returns `EFI_INPUT_KEY`. **The firmware phase needs no guest IDT, no PIT/LAPIC emulation and no interrupt delivery — we signal the wait.** Test app v2 exercises `Stall → CreateEvent → SetTimer(100 ms) → WaitForEvent → AllocatePages → OutputString` with distinct OK/FAIL `hlt` addresses. Closes on nested serial `RAYNU-V-RAYNU-F-TIMER-OK` + `RAYNU-V-RAYNU-F-MEM-OK` + `HLT … path=OK` (guest-exit-only). Not implemented (honest): notify-function dispatch into the guest, event groups, sub-page pool. |
| **F4** | Handle/protocol DB + BlockIo | QEMU → iron | **HOST-PROVEN; nested run publishes media (`BlockIo cd_bytes=66060288 cd_last_lba=32255 disk_bytes=1048576`), guest `BLOCKIO-OK` still awaited (GRUB has not reached a block read yet).** `RAYNU-V-RAYNU-F-BLOCKIO-GATE-OK`. **Protocol database:** real UEFI GUIDs (BlockIo `964E5B21-…`, SimpleFs, LoadedImage, DevicePath), opaque tagged handles, `HandleProtocol`, `OpenProtocol`, `CloseProtocol`, `LocateProtocol`, `LocateHandle` (sizing + fill), `LocateHandleBuffer` (allocates from our pool), `InstallProtocolInterface` — so a loader finds storage the architected way instead of us guessing. **BlockIo:** two `EFI_BLOCK_IO_PROTOCOL` instances (rev 2) sharing one trampoline set, `This`/`MediaId` selecting device — CD (id 1, 2048 B, read-only, removable, backed by the retained product ISO) and install disk (id 2, 512 B, read-write, backed by virtio-blk); byte-exact `EFI_BLOCK_IO_MEDIA` (0x30); spec §13.9 validation (`NO_MEDIA`, `MEDIA_CHANGED`, `WRITE_PROTECTED`, `BAD_BUFFER_SIZE`, past-end, overflow-safe LBA); `ReadBlocks`/`WriteBlocks` chunked through guest memory to `fn`-pointer backing stores, `Reset`/`FlushBlocks`. Host test reads a real ISO9660 PVD (`\x01CD001` at LBA 16) and round-trips an `EFI PART` write to the disk — the path a real installer's GPT write takes. Closes on nested `RAYNU-V-RAYNU-F-BLOCKIO-OK` (guest-exit-only). Not modelled (honest): driver binding/`ConnectController`, protocol notify, by-child/by-driver `OpenProtocol` bookkeeping. |
| **F5** | SimpleFileSystem + LoadImage/StartImage of the ISO's `\EFI\BOOT\BOOTX64.EFI` | QEMU → iron | **F5c LAUNCH PROVEN on nested `raynuvsrv1` (2026-09-04, `7ee3a3b`): `FAT ESP mounted lba=77 efi=1` → `found \EFI\BOOT\BOOTX64.EFI bytes=724992` → `ISO bootloader staged base=0xbb1000 entry=0xbb2000 relocs=1851` → `VMLAUNCH … image=ISO-BOOTX64` → GRUB itself ran on RayNu-F tables: 17 `ConOut.OutputString` calls printed GRUB's own `Could not malloc` / `Aborted. Press any key to exit.` through our console, then it polled our `ConIn.ReadKeyStroke` until the exit cap. Guest-side FS read (`RAYNU-F-FS-OK`) still awaited — GRUB died in `mm_init` before opening a file, on two RayNu-F bugs fixed in F6-prep below.** Host:  F5a `RAYNU-V-RAYNU-F-FS-GATE-OK`: FAT12/16/32 reader (BPB with cluster-count-derived type, nibble-packed FAT12, chain traversal with hop cap, fixed-root and FAT32 dir walks, 8.3 names, `\`-or-`/` paths). F5b `RAYNU-V-RAYNU-F-IMAGE-GATE-OK`: `EFI_SIMPLE_FILE_SYSTEM_PROTOCOL.OpenVolume` + `EFI_FILE_PROTOCOL` (Open/Close/Read/GetPosition/SetPosition/GetInfo/Flush; one protocol instance per open-file slot so `This->Read(This,…)` is correct); `pe::load_pe32plus_guest` mirrors the loader over `GuestMem`; `LoadImage(SourceBuffer)` allocates from our pool, relocates against the new base, publishes `EFI_LOADED_IMAGE_PROTOCOL`; `StartImage` redirects the guest to the image entry with `RCX=ImageHandle`/`RDX=*SystemTable`, and because the caller's return address is still at `[RSP]` at the `out`, the image's own `ret` lands back after the call. F5c: the **launcher now boots the ISO's real bootloader** — parse El Torito, mount the FAT ESP, resolve `\EFI\BOOT\BOOTX64.EFI`, stage and load it from our pool, and launch *that* as the guest; the built-in test app is the fallback when there is no ISO / not FAT / no loader. Read-only volume: Write/Delete/SetInfo are `EFI_WRITE_PROTECTED`. Honest gaps: LFN lookup (product path is 8.3-clean), directory enumeration, `Exit`/`UnloadImage`, DevicePath-based `LoadImage`. Closes on nested `RAYNU-V-RAYNU-F-START-IMAGE-OK` + `RAYNU-F-FS-OK` with the real loader running. |
| **F6-prep** | Real RAM for the loader + LoadedImage at launch | QEMU | **HOST-PROVEN (`raynu_f_high_ram_and_launch_loaded_image`), awaiting nested rerun.** What GRUB named on `7ee3a3b`: (1) `OpenProtocol(ImageHandle, LoadedImage)` → `EFI_NOT_FOUND` ×2 — the launcher staged GRUB directly and only `LoadImage` published `EFI_LOADED_IMAGE_PROTOCOL`; now `publish_loaded_image()` runs on the F5 stage path too. (2) `AllocatePool` → `OUT_OF_RESOURCES`, `AllocatePages(AllocateAddress)` → `NOT_FOUND`: GRUB `mm_init` wants a 32 MiB heap and `AllocateAddress`-walks every conventional descriptor; our pool was 20 MiB and the map advertised slab slack as conventional that the allocator did not own. `PagePool` now spans the slab pool **plus a high region over the report-RAM the product-ISO path already EPT pre-maps contiguously from 32 MiB** (only pre-mapped slots, since RayNu-F stops on EPT violation; capped at 256 MiB), and the map advertises as conventional exactly what `AllocateAddress` can hand out (slab slack → `EfiBootServicesData`). **Memory-map honesty rule:** every conventional byte must be allocatable at its own address. Diagnostics: failed service lines carry `a1..a4` and the GUID for protocol lookups. Closes when nested GRUB gets past `mm_init` (menu or `FS-OK`). Known next gaps GRUB will name: `LocateProtocol` for GOP/SimpleTextInputEx (tolerated), runtime `GetVariable` (`EFI_UNSUPPORTED`, tolerated), directory enumeration, `Exit`. |
| **F6** | Installer to disk | iron | `RAYNU-V-M7-ISO-INSTALL-OK` — the only E5 close. |

Fast harness is nested/QEMU; iron flashes only for iron-specific confirmation
(MTRR/GCD/phys-width, real-silicon LAPIC/timer). No stage closes on host/CI
prints alone past F1.

## Anti-self-wound rule (why this won't recur)

The governance constraint (#4) is the durable fix. A change is a **self-inflicted
wound** when it makes forward progress by editing a component's private state
that we do not own the invariants for. The tell in the record: a fix that
"forces X to return / advances RIP past a check / pokes a gState / injects a
fault to steer flow" — each of those in `3k–3o` created the next hang. RayNu-F
removes the category: we own every structure, so there is nothing foreign to
corrupt, and every wait completes because *we* signal it.

## References

- [ADR-002](ADR-002.md) — Proven Core boundary (guest firmware is outside)
- [ADR-003](ADR-003.md) — size budget for embedded firmware assets
- [ADR-009](ADR-009.md) — Mount Everest E5
- [ADR-014](ADR-014.md) — typed ISO + UEFI+virtio product boot (RayNu-F fulfils it)
- [ADR-015](ADR-015.md) — Stage 46 close path (OVMF mechanism, now superseded for E5 close)
- Plan: [`docs/m7_plan.md`](../m7_plan.md) · ladder: [`docs/stage46_everest_plan.md`](../stage46_everest_plan.md)
