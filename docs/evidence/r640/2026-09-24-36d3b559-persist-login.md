# 2026-09-24 — `36d3b559`: installed login, twice

EFI `36d3b5595d67` (`cursor/m8-grubcfg-esp-8366`). Lease `10.99.99.148/24`
on every boot of this sha. Front USB 2 UDisk. Toshiba `0480:a004` on xHCI
p11. `usbsoak.txt` stayed off.

## What the old disk was

The first product boot of this EFI, still on the partial install, printed
`grubcfg=no`, staged `BOOTX64.EFI bytes=139264`, and stopped at GRUB 2.12
`grub>`. Ubuntu read of that volume: no `/boot/grub/grub.cfg`, 279 modules
under `/boot/grub/x86_64-efi`, and `grep -c "Minimal BASH-like"` of
`BOOTX64.EFI` was 0. The prompt was a missing menu file, not a USB miss.
That GPT was zeroed (LBA 0–33 and the 8 GiB backup GPT only).

## Blank-disk install

Empty `raynuf.txt` was removed. SPA Start (`POST /vms/1/spec/2/2048/8192/1/linux_iso`
then `POST /vms/1/start`) on blank media printed `image=ISO-BOOTX64`,
`[vda] 16777216`, auto-answer `setup-disk`, `RAYNU-V-M7-ISO-INSTALL-OK`,
`Installation finished. No error reported.`, and
`Installation is complete. Please reboot.` The live installer then sat at
`localhost:~#`. Force Off. Ubuntu `lsblk` showed the Toshiba 48M vfat + 8G
ext4. Empty `raynuf.txt` was put back (`sudo touch`).

## Persist boot 1 and boot 2

Both boots, no curl:

```
boot: RayNu-F launch requested (EFI/RayNu/raynuf.txt; ADR-016 F2b; not ISO-INSTALL-OK)
peek … efi=EFI PART gpt=1 fit=1 … guest=8589934592 bootx64=1 ext4=1 installed=1
boot: Stage 46 virtio-blk install disk bytes=8589934592 keep=1
boot: Stage 46 disk past pin lba=2048 n=512 ok
boot: RayNu-F found \EFI\BOOT\BOOTX64.EFI bytes=139264
boot: RayNu-F grubcfg=no (F7 disk; not ISO-INSTALL-OK)
image=DISK-BOOTX64
RAYNU-V-RAYNU-F-DISK-BOOT-OK
RAYNU-V-M8-DISK-PERSIST-OK
```

GRUB 2.12 printed `error: no suitable video mode found` and
`error: unable to determine partition UUID of boot device`, then the menu
`Alpine Linux v3.21, with Linux lts` and auto-booted. `grubcfg=no` is the
ESP lookup: Alpine keeps `grub.cfg` on the ext4 `/boot/grub`, not on the
ESP. Kernel cmdline:

```
root=UUID=a0ad99ac-ca25-4458-ade1-cd8599ca4928 … modules=sd-mod,usb-storage,ext4 …
```

`[vda] 16777216` (8.00 GiB), `vda1` + `vda2`. `[vdb] 2035712` is the ISO.
Both boots reached `localhost login: root` and `cat /proc/cmdline` showed
that UUID.

Boot 2, after another Force Off, needed ext4 journal recovery
(`recovery required on readonly filesystem` → `recovery complete`, same
UUID) and fsck cleared a vfat dirty bit (boot sector vs backup at offset
65, `01/00`, “mostly harmless”). `/dev/vda2` was
`clean, 6380/521216 files, 102908/2084352 blocks` (boot 1 was
`6377/521216`, `102901/2084352`). Clock skew is CMOS stuck at 2026-08-16.

## What it means

The installed 8 GiB slice survived two hypervisor Force Offs on one EFI and
came back to the same login. That is the repeat A2 was waiting on. It is
not PERC, not the whole Toshiba (`bytes=320072933376` stays outside the
virtio window), and not A4s: no browser was opened on these persist boots.
Do not reflash. Do not curl Start. Do not run `setup-disk`.

## Boot 3, after the chassis turned off

Firefox rendered `https://raynu-v.lab:8443` on boot 2 (lease `.148`). iDRAC
Lifecycle then logged `SYS1003` “System CPU Resetting” at 20:14:53 and
`SYS1001` “System is turning off” at 20:14:54. No power-button, watchdog, or
user power-control line was in that view. Boot 3 of the same EFI leased
`10.99.99.150/24` and printed `keep=1`, `grubcfg=no`, `image=DISK-BOOTX64`,
`RAYNU-V-M8-DISK-PERSIST-OK`, the GRUB menu, and `localhost login:`. Ext4
journal recovery completed on the same UUID. `/dev/vda2` was
`clean, 6380/521216 files, 102908/2084352 blocks`, the same counts as boot 2.
vfat dirty bit offset 65 `01/00` was cleared again. `cat /proc/cmdline` at
the shell showed `root=UUID=a0ad99ac-ca25-4458-ade1-cd8599ca4928`.

## Second SPA death, 23:45

Firefox rendered `https://raynu-v.lab:8443` again on boot 3 (hosts pointed
at `.150`, lock icon, Host green, “No guest yet”). COM2 was still at
`localhost:~#`. iDRAC Lifecycle then logged:

| Seq | Time | ID | Message |
|-----|------|----|---------|
| 22501 | 23:45:21 | SYS1003 | System CPU Resetting |
| 22502 | 23:45:22 | SYS1001 | System is turning off |

FQDD `iDRAC.Embedded.1#HostPowerCtrl`, AgentID `DE`. No `RAC1195`, no
`RAC0701` / `RAC0703`, no watchdog line, no power-button line between the
SSH login at 23:27:57 (seq 22500) and that pair. The sequence numbers are
contiguous.

User power actions in the same view look different:

- 19:52:18 seq 22478 `RAC1195` + seq 22479 `RAC0703` “Requested system
  hardreset”, then seq 22480 `SYS1003` only. The machine stayed in reset.
- 23:26:42 seq 22491 `RAC1195` + seq 22492 `RAC0701` “Requested system
  powerup”, then seq 22493 `SYS1000` “System is turning on”.
- 23:26:58 seq 22497 `SYS1003` alone, about 13 s after power-up. Boot CPU
  reset. No `SYS1001`. The guest reached `login:`.

Both SPA deaths (20:14:53 and 23:45:21) are `SYS1003` then `SYS1001` one
second later, with no GUI power-control line in front.

## Boot 4 — `fa6ce771` after the 23:45 power-off

`fa6ce771557b` (Linux resume SOL RX paced) leased `10.99.99.150/24` again.
PRE-EBS `http://` timed out (no curl). Post-EBS printed
`CURL NOW → https://10.99.99.150:8443/`. Persist path:

```
keep=1 guest=8589934592 installed=1
disk past pin lba=2048 n=512 ok
BOOTX64.EFI bytes=139264
grubcfg=no
image=DISK-BOOTX64
RAYNU-V-RAYNU-F-DISK-BOOT-OK
RAYNU-V-M8-DISK-PERSIST-OK
```

GRUB menu auto-booted. `[vda] 16777216`. Cmdline
`root=UUID=a0ad99ac-ca25-4458-ade1-cd8599ca4928`. Ext4 journal recovery,
then `/dev/vda2: clean, 6380/521216 files, 102908/2084352 blocks` — the
same counts as boots 2 and 3. vfat dirty bit offset 65 `01/00` cleared
again. `localhost login: root`, then `cat /proc/cmdline` at the shell.
The 23:45 chassis-off left the install in place. One Firefox tab then
painted `https://raynu-v.lab:8443` (Host green, “No guest yet”) and the
iDRAC dashboard showed Power State OFF, system healthy, virtual console
No Signal. Paced Linux-resume SOL RX did not stop it. This is the third
SPA power-off. Quit Firefox. Do not boot `fa6ce771` to open the page
again. Do not curl. Do not `setup-disk`.

The 00:30 dashboard off is now in the lifecycle log: seq 22520 `SYS1003`
and seq 22521 `SYS1001` at 2026-09-25 00:30:20. No `RAC1195`.

## Boot 5 — `3e9ce45e` listen hold, then the same power-off

`3e9ce45e3c58` leased `10.99.99.151/24`. PRE-EBS `http://` timed out.
Persist path matched boots 2–4: `keep=1`, `grubcfg=no`, `DISK-BOOTX64`,
`RAYNU-V-M8-DISK-PERSIST-OK`, `[vda] 16777216`,
`root=UUID=a0ad99ac-ca25-4458-ade1-cd8599ca4928`. Ext4 journal recovery.
`/dev/vda2: clean, 6380/521216 files, 102909/2084352 blocks` (one block
above boots 2–4). vfat dirty bit offset 65 `01/00` cleared again.
`localhost:~#`, then `cat /proc/cmdline`.

Firefox painted the SPA. Host went red (“List failed”), then green
(“Server is answering”, “Empty (create one)”), then red again. COM2’s
last line stayed `virtio MMIO … n=4086`. `write_line` is dropped once
Linux shares the UART, so `HTTP exchange ok` and `listen hold after HTTP`
could not appear. The green card is still a completed `GET /vms`.

12:21:23 seq 22543 `SYS1003` and seq 22544 `SYS1001`, same second. No
`RAC1195`. The 12:05:45 hard reset is seq 22534 `RAC1195` + seq 22535
`RAC0703` + seq 22536 `SYS1003` only. The 11:46:42 Power On is seq 22525
`RAC1195` + seq 22526 `RAC0701` + seq 22527 `SYS1000`. This is the fourth
SPA power-off. Quit Firefox. Do not boot `3e9ce45e` to open the page.
The 30 s re-listen is what let Host go green. The next EFI does not
re-listen after one exchange.

## Boot 6 — `1f33eeda` keep-alive, A4s lived

`1f33eeda72f9` (CI `36137732145`, EFI SHA256
`5539d83806e120eb6c331c11281407c0f902b121a770c7faa46d6a1a26280693`)
leased `10.99.99.154/24`. Persist path matched boots 2–5: `keep=1`,
`installed=1`, `guest=8589934592`, `diskprime lba1=pin warm=1`,
`disk past pin lba=2048 n=512 ok`, `BOOTX64.EFI bytes=139264`,
`grubcfg=no`, `image=DISK-BOOTX64`, `RAYNU-V-RAYNU-F-DISK-BOOT-OK`,
`RAYNU-V-M8-DISK-PERSIST-OK`. GRUB menu auto-booted. `[vda] 16777216`.
`root=UUID=a0ad99ac-ca25-4458-ade1-cd8599ca4928`. Ext4 journal recovery.
`/dev/vda2: clean, 6380/521216 files, 102909/2084352 blocks` (same as
boot 5). vfat dirty bit offset 65 `01/00` cleared again.
`RAYNU-V-M8-TLS-OK` did not print (no pre-guest HTTPS exchange).

One `boot: HOST-NIC TCP accept — client connected`, then `HTTP exchange ok`
/ `HTTP keep-alive` through Overview polls and the Activity tab. No second
`TCP accept`. No `TCP re-listen after HTTP`. Host stayed green (“Server is
answering”). Activity showed Alpine `localhost login:` and the motd, then
the console tail also showed host `HTTP keep-alive` lines because those
use `write_line_nowait` on the UART the guest log copies. The chassis
stayed up. No new lifecycle power-off line was pasted for this boot.

This is A4s. It is not a new A2 close and not A6. The Send box stays
empty on this EFI: `RAYNU-V-M8-CONSOLE-OK` still uses `write_line` and
is hushed after Linux shares the UART. Rollback kit: `v0.1.0-m8-a4s`.
Do not flash over this live page. Do not curl. Do not `setup-disk`.
