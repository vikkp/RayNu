STATUS=closed
# E5 / M7.7 ISO install-to-disk — CLOSED on iron (2026-08-16 stamp persist;
# 2026-09-10 real distro install).
#
# 2026-09-10 — real Alpine install on R640 (RayNu-F, Stage 46 product ISO):
#   Firmware printed the literal RAYNU-V-M7-ISO-INSTALL-OK on R640 COM2
#   after `setup-disk -m sys /dev/vda` → "Installation finished. No error
#   reported." (GPT + grub x86_64-efi + initramfs on the virtio disk).
#   EFI 59ac070, CI run 34425781629, LogiLink UDisk front USB 2.
#   Evidence: docs/evidence/r640/2026-09-10-59ac070-iso-install-ok.md
#   Reboot-to-disk on iron — CLOSED 2026-09-10 (EFI 56a3ffd, CI run
#   34480107961, same UDisk): `reboot` -> F7 relaunch -> GPT ESP -> installed
#   GRUB 2.12 countdown -> RAYNU-V-RAYNU-F-DISK-BOOT-OK -> second
#   `Linux version 6.12.13-0-lts` root=UUID=9af18543-... -> EXT4 vda2 ->
#   OpenRC -> login: -> /proc/cmdline from the installed system.
#   Evidence: docs/evidence/r640/2026-09-10-56a3ffd-e5-reboot-to-disk-disk-boot-ok.md
#   History: 59ac070 F7 relaunch failed VMCLEAR/VMPTRLD (host-stack overflow
#   into the VMCS); 975f8fc relaunched into the GRUB menu then hit the fixed
#   1 M exit-count cap; 56a3ffd bounds the loader phase by wall time.
#   E5 Phase A (ISO -> installer -> virtio disk -> reboot to disk) is closed
#   on iron. Phase B (SPA/REST starts that disk) is the E4 residual.
#
# 2026-08-16 — stamp persist (first iron close, superseded in kind):
# Iron close (documented equivalent of RAYNU-V-M7-ISO-INSTALL-OK):
#   Firmware printed RAYNU-V-M7-ISO-BOOTED-FROM-DISK on R640 COM2
#   after persist-detect + 1KiB prefix into 64MiB virtio.
#   Evidence: docs/evidence/r640/2026-08-16-e5-iso-install.md
#   Boot2 log: logs/2026-08-16-e5-booted-from-disk-com2.txt
#   Boot1 write: logs/2026-08-16-e5-persist-write-com2.txt
#   Prefix-miss residual: 2026-08-16-e5-persist-detect-blk-fail.md
#   Media: Cruzer Micro front USB 2; EFI SHA 2d931dcd…f4281eed (Mac)
#   Host/CI never prints RAYNU-V-M7-ISO-INSTALL-OK (scaffold only).
#
# Honesty: LBA stamp persist, not a guest filesystem / distro installer.
# Mount Everest remains open (post-EBS mgmt HTTP first; then E4 polish + distro).
#
# Scaffold (host/CI): RAYNU-V-M7-ISO-INSTALL-SCAFFOLD-OK
#   ./tools/m7-iso-install-smoke.sh
#
# QEMU lab (not the iron close):
#   RAYNU-V-M7-ISO-INSTALL-LAB-OK + RAYNU-V-M7-ISO-BOOTED-FROM-DISK
#
# Prior:
#   M7.3 RAYNU-V-M7-ISO-OK — deploy plan smoke only
#   E2 RAYNU-V-R640-BOOT-OK — 2026-08-15
#   E3 RAYNU-V-M7-UEFI-HTTP-OK — 2026-08-16
#   E4 SPA create + install arm — 2026-08-16
