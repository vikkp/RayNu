# Iron COM2 — `61991be` HLT `cf8=0x0` still `ataio=0`

- **Date:** 2026-09-02
- **Operator:** vikkp @ raynuvsrv1
- **Boot:** F11 Cruzer Micro (front USB 2), iDRAC `console com2`
- **EFI:** CI `--run 33573126367` (`61991be`, sha256 `12f84c66…`)
- **Media:** `OVMF.fd` 3653632 · `linux.iso` 66060288
- **Does not claim:** `RAYNU-V-M7-ISO-INSTALL-OK` · Linux `ACPI=` · El Torito · Everest E5

## What moved

| Line | Meaning |
|------|---------|
| `HLT … cmdwr=6 pcicmd=0x5 wr=0x0 seq=0,0,0,0,0,0 cf8=0x0` | Last CONFIG_ADDRESS write is 0 |
| ticks `rip=0x7f01fc0f` `port=0xcf8` `insn=ef` | Firmware OUT to CF8 before HLT |
| `HLT wait-for-irq rip=0x7f0680d0` | Same CpuSleep as `c144001` |

Firmware finished the PCI walk and wrote CONFIG_ADDRESS **0** (deselect). It is not stuck mid-ParseBar on an IDE BDF. EnableAttributes `pcicmd=0x5` still holds.

## What did not

No `Linux version`, no `efi:` / `ACPI=`, no `ataio>0`, no `sectors>0`.

After skip-HLT, ticks stay on CpuSleep:

- `reason=0xc` `rip=0x7f0680d0` `insn=f4c3` `inj=1`
- `ataio=0` `cmd=0x00` `pin14=0` through `n>1.7M`

`cmd=0x00` is last ATA 0x1F7, not PCI COMMAND. `cf8=0x0` is the last write, not the last **enabled** CF8 (bit 31).

## COMMAND still closed

| Pin | Stored COMMAND | Last CF8 | `ataio` |
|-----|----------------|----------|---------|
| `184ee61` | `OR 0x0001` → `pcicmd=0x1` | (not printed) | 0 |
| `abba969` | honor → `pcicmd=0` | (not printed) | 0 |
| `060c504` | seq all `0` | (not printed) | 0 |
| `c144001` | EnableAttributes → `pcicmd=0x5` | (not printed) | 0 |
| `61991be` | EnableAttributes → `pcicmd=0x5` | `cf8=0x0` | 0 |

Do not OR `0x0001`. Do not resume #231 for further `COMMAND.IO` (ADR-015). HLT policy is still exhausted. Do not F11 `61991be` / `--run 33573126367` again.

## Next SHA

Latch last CF8 with enable bit 31 (`LAST_CF8_EN`). Print `cf8en=` next to `cf8=` on the HLT stall and the stop line. Proof: `cf8=0x0 cf8en=0x8000xxxx` (last live BDF), then `ataio>0` or the same hang. Still not `ISO-INSTALL-OK`.

## Ladder

| Step | Result |
|------|--------|
| honor COMMAND as written | **DONE** (`abba969`) |
| six-write sequence | **DONE** (all `0`) |
| EnableAttributes `0x0005` after write-0 | **DONE** (`pcicmd=0x5`) |
| last PCI CF8 on HLT | **DONE** (`cf8=0x0`) |
| 3b ATA / El Torito / Linux `ACPI=` | **FAIL** (`ataio=0`, same hang) |
| 3c last enabled CF8 | **THIS SHA** |
