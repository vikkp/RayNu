# Iron COM2 — `0b770cd` HLT `rethx=0xe056ff41b84d8b48` still `ataio=0`

- **Date:** 2026-09-03
- **Operator:** vikkp @ raynuvsrv1
- **Boot:** F11 Cruzer Micro (front USB 2), iDRAC `console com2`
- **EFI:** CI `--run 33701350767` (`0b770cd`, sha256 `b17314b7…`)
- **Media:** `OVMF.fd` 3653632 · `linux.iso` 66060288
- **Does not claim:** `RAYNU-V-M7-ISO-INSTALL-OK` · Linux `ACPI=` · El Torito · Everest E5

## What moved

`rethx=0xe056ff41b84d8b48` printed next to `ret=0x7ff0e055`.
Little-endian bytes `48 8b 4d b8 41 ff 56 e0`:

```
mov rcx, qword ptr [rbp-0x48]
call qword ptr [r14-0x20]
```

That is DxeCore WaitForEvent invoking CpuSleep (`rip=0x7f0680d0`
`insn=f4c3`) through a function pointer. Callsite named.

## What did not

No `Linux version`, no `efi:` / `ACPI=`, no `ataio>0`, no `sectors>0`.

Same hang after the print:

- HLT `ret=0x7ff0e055` `rethx=0xe056ff41b84d8b48` `ataio=0`
- ConIn CR still fired; PIT one-shot + `inject vec=0x20` still `inj=1`
- Through `n>1.2M` `reason=0xc` `rip=0x7f0680d0`

CpuSleep returns (HLT skip after one-shot). WaitForEvent loops.
The event never signals.

## Ladder

| Pin | Result | `ataio` |
|-----|--------|---------|
| `2d4ab51` | `ret=0x7ff0e055` (DxeCore) | 0 |
| `6c4bfde` | ConIn CR; timer wait | 0 |
| `0b770cd` | `call [r14-0x20]` CpuSleep | 0 |

Do not F11 `0b770cd` / `--run 33701350767` again. Keep hide-slot0.
Keep ConIn CR. Do not hide PIIX. Do not start another dest / HLT /
COMMAND / CF8 / ROM / hide-slot0 / ConIn / callsite SHA.

## Next SHA

Unwind that WaitForEvent as `EFI_SUCCESS` (`xor eax,eax; leave; ret`)
and print `caller=`. Still not `ISO-INSTALL-OK`.
