# Iron M8.1 TLS-OK — native HTTPS before RayNu-F (`928d6224`)

**Status: CLOSED on COM2 2026-09-19.**  
**EFI:** `build: sha=928d62241967` (`cursor/m8-tls-iron-8366`).  
**Not this close:** PRE-EBS SNP `http://`. Nested QEMU. Host `RAYNU-V-M8-TLS-HOST-OK` / `TLS12-HOST-OK` / `TLS-FW-HOST-OK`. rustls/ring in `uefi-bin`.

## Sequence

1. UDisk F11. PRE-EBS SNP `http://10.99.99.140:8443/` window timed out (expected; operator did not curl plaintext).
2. ExitBootServices. Toshiba p11 BOT ready. Peek `keep=1` `installed=1` virtio 8 GiB. Leftover DRAM skipped.
3. Native BCM5720 analog `01:00.00` MAC `b0:26:28:5c:5a:38` `link=up`.
4. `CURL NOW → https://10.99.99.140:8443/` `(native BCM5720; before RayNu-F; SNP is dead)`.
5. Mac:

```bash
LEASE=10.99.99.140
curl -4 --noproxy '*' -sS -m 20 --tlsv1.2 --tls-max 1.2 \
  --ciphers ECDHE-RSA-AES128-GCM-SHA256 \
  --cacert /tmp/raynu-lab-ca.crt.pem \
  --resolve "raynu-v.lab:8443:${LEASE}" \
  "https://raynu-v.lab:8443/" | head
```

Returned SPA `<!DOCTYPE html>` / `<title>RayNu-V</title>`.

6. COM2:

```
boot: HOST-NIC TCP accept — client connected
boot: HOST-NIC HTTP exchange ok
RAYNU-V-M8-TLS-OK
```

Then RayNu-F `image=DISK-BOOTX64` → `RAYNU-V-RAYNU-F-DISK-BOOT-OK` → **`RAYNU-V-M8-DISK-PERSIST-OK`** → Alpine `root=UUID=dd673a9a-30c3-4acd-8d7e-9afe9313c46f` → `localhost:~#`.

## Honesty

- Lab millicert CA, TLS 1.2 ECDHE-RSA-AES128-GCM only. Not production PKI. Not TLS 1.3.
- PRE-EBS SNP remains `http://`.
- SPA `GET /` is still unauthenticated. Iron AUTH-OK is open.
- Persist-OK printed on this keep=1 DISK-BOOT. Guest window is still the 8 GiB Toshiba slice. USB ≠ PERC. UUID `dd673a9a` is this image, not A2 `348005a9`.
- Sit at `localhost:~#`. Do not `setup-disk`. Do not flash Toshiba `/dev/sdc`.
