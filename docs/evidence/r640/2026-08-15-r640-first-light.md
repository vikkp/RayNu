# R640 first-light evidence — 2026-08-15

Partial iron attempt. **Does not close** `RAYNU-V-R640-BOOT-OK` (E2).  
Records real PowerEdge R640 **M0** first light and the post-M0 residual.

| Field | Value |
|-------|-------|
| Date (UTC) | 2026-08-15 |
| Operator | CNP (operator laptop + iDRAC9 Enterprise) |
| Host (service tag / hostname) | Dell PowerEdge R640 (service tag TBD in follow-up) |
| Boot method | iDRAC Virtual Media — `raynu-v-0.1.0-uefi-boot.img` mapped as removable / Virtual Floppy |
| EFI path on media | `\EFI\BOOT\BOOTX64.EFI` |
| `r640-hypervisor.efi` SHA256 | `1c7a9be01f1a70852e4965744b5334b556e0ce26f33ccaa2dd648e9d5bece1c3` |
| Release kit version | 0.1.0 (`releases/v0.1.0` / media maker from kit) |
| iDRAC firmware (if known) | iDRAC9 Enterprise (web UI) |
| BIOS / boot mode | UEFI; one-shot boot → Virtual Floppy |
| Serial channel | iDRAC HTML5 Virtual Console (**ConOut / video**). SOL enabled in iDRAC Settings → Connectivity (115.2 kbps) but **no live SOL transcript captured** this session (SSH `console com2` / `ipmitool sol activate` not completed). |

## Required serial markers

- [x] `RAYNU-V-M0-BOOT-OK` — observed on iDRAC Virtual Console
- [ ] VMX path (`RAYNU-V-M1-VMXON-OK` / `RAYNU-V-M1-VMEXIT-OK`) — **not observed**
- [ ] EPT / guest progress — **not observed**

### Documented residual

After `RAYNU-V-M0-BOOT-OK`, the Virtual Console stayed on M0 with a cursor for an extended period. That matches **ConOut freezing after ExitBootServices**: post-M0 progress is printed only on **COM1**. Operator could not obtain a live SOL session this attempt, so markers past M0 are **unknown / unproven** — do **not** invent them.

## Serial excerpt (Virtual Console / ConOut)

```text
Booting from Virtual Floppy Drive
RayNu-V r640-hypervisor formally verified bare-metal hypervisor
RAYNU-V-M0-BOOT-OK
```

(No further ConOut lines. No SOL capture attached.)

## Iron close claim

**Not claimed.** `RAYNU-V-R640-BOOT-OK` and `GAP(CLOSED M7.5)` remain open until COM1/SOL shows VMX+EPT path (or an equivalent filled residual with later markers) on real R640.

This file is **first-light M0 evidence only**.
