# Runbook — M8.0 iron DurableLun (PCI pick → post-EBS I/O → Phase B attach)

**Iron marker:** `RAYNU-V-M8-DISK-PERSIST-OK` (const-only; **not serial-printed**). A2 **CLOSED on evidence** (`4af78b43` keep=1 + `DISK-BOOTX64` + `UUID=348005a9-…` after Force Off).  
**Nested marker:** `RAYNU-V-M8-DISK-PERSIST-NESTED-OK` (QEMU File RAM — **not this runbook**)  
**Host marker:** `RAYNU-V-M8-DISK-PERSIST-HOST-OK` (cargo — **not this runbook**)  
**Never print:** `RAYNU-V-M7-ISO-INSTALL-OK`  
**M8.0 known-good flash:** GitHub Latest [`v0.1.0-everest-closed`](https://github.com/vikkp/RayNu/releases/tag/v0.1.0-everest-closed). Do not F11 a persist prototype until COM2 shows I/O ready + virtio on the LUN.

Nested QEMU ≠ R640. QEMU NVMe/USB ≠ Intel PCH xHCI. Nested File RAM (`M8_PERSIST_IMG` `share=on`) does not exist on iron.

---

## Hardware (required before F11)

| Role | What | Notes |
|------|--------|--------|
| ESP | Cruzer / UDisk 2–8 GiB with alpine-extended | Boot stick. Mapper **refuses** this window as a LUN. |
| LUN | NVMe class `01:08`, **or** USB ≥ 1 GiB **outside** 2–8 GiB | 16 GiB+ stick is the usual USB pick. 1–2 GiB also eligible. never PERC. |
| Standing boot | Ubuntu on PERC | Do **not** format PERC. One-time F11 the ESP only. |

Without a LUN, COM2 prints `durable LUN none (leftover DRAM)` / `skip PERC` / `skip ESP Cruzer` plus `need NVMe class 01:08 or USB ≥1GiB outside Cruzer 2-8GiB`. Phase B then installs to leftover DRAM (dies on Force Off).

---

## COM2 needles (this slice)

Open iDRAC **SOL `console com2` first**.

### 1. PCI pick

Expect storage-class lines, then a pick or a reject:

- `durable LUN pci … nvme` → pick NVMe (`01:08`)
- `skip PERC` / `skip AHCI` / `skip ICH9 SATA` / `skip IDE` — not LUNs
- `durable LUN xhci … 8086:a1af` (Lewisburg) — USB I/O after EBS
- `durable LUN picked nvme|usb …`
- or `durable LUN none (leftover DRAM)` + the need-media line

### 2. Post-EBS I/O

After ExitBootServices:

- `durable LUN nvme I/O ready bytes=…` **or**
- `durable LUN usb I/O ready bytes=…` (ESP Cruzer window skipped; next port)
- `xhci cap caplen=… slots=… ports=… scratch=…/64 csz=` on **every** USB attempt (before PORTSC)
- `xhci cap … scan=… p1=0x…` (first 16 PORTSC words) then `xhci ccs pN=0x…` for every CCS port (Lewisburg 26 ports, not a 16-port walk)
- `xhci enum pN sc=0x… speed= usb3= csz= cmd= cmpl=0x…` on Enum fail (cmd `1` reset `2` slot `3` addr `4` desc `5` cfg/`6` SET_CONFIG). `xhci setcfg pN val=` means SET_CONFIGURATION completed before Configure Endpoint. `cmpl=0x11` is Parameter Error. `cmpl=0xff` + `timeout` means the command never completed (not a real CC). Iron `3473a0b9` `cmpl=0x40` was **MaxSlots**, not CC 64. COM2 fail lines still print `bot=cbw|data|csw` and `scsi=`. `BULK_SPINS` bounds bulk Transfer Event waits. Cap line prints `hcc1=` next to `csz=`.
- After Address Device: `xhci enum pN vid= did= class= proto=` so UDisk vs Toshiba is named. `xhci eval pN mps=` is Evaluate Context EP0 MPS (cmd type 13). `xhci bot pN iface=08/06/50 ep_out= ep_in=` is the config-descriptor parse (MSC 08/06/50 lives on the interface, not the device descriptor). `xhci cfg-skip pN toshiba` means GET_CONFIG failed and the iron `96024edc` Toshiba BOT layout was used. `xhci epst pN ep0= out= in=` is Output Context EP State after Configure Endpoint (0 Disabled, 1 Running, 2 Halted, 3 Stopped). `xhci botrst pN` is MSC Bulk-Only Reset + Clear Halt before GET_MAX_LUN. `xhci maxlun pN skip` is skipping optional GET_MAX_LUN so EP0 stays Running. `xhci maxlun pN val=` was Get Max LUN after BOT reset (stall is not fatal; Iron norearm COM2 `val=0` then `epst ep0=3`). `xhci firstcbw pN overlap` is INQUIRY CBW OUT and DATA IN armed before waiting. `xhci capoverlap pN` is overlapping CAPACITY the same way. `xhci cswqueue pN` is CSW IN queued on the bulk IN ring with DATA (not a sequential 13-byte CSW after DATA). `xhci writequeue pN` is WRITE CBW OUT + DATA OUT + CSW IN queued together (not sequential WRITE CBW). `xhci writesettle pN` is extra settle before the first guest WRITE (READ→WRITE direction switch). `xhci nopretry` is a second No-Op after abort when the first timed out (Iron cswsettle COM2 leftover ADDR). `xhci addrretry pN` is Address Device retry on the Enabled slot after abort (do not Enable Slot again; do not walk to p14). `xhci setcfgretry pN` is SET_CONFIGURATION retry on the live slot after abort (Iron okquiet COM2 leftover SET_CONFIG `cmd=6 cmpl=0xff`; do not Disable Slot; do not walk to p14). `xhci config pN` is Configure Endpoint after SET_CONFIG. `xhci cfgretry pN n=` is a second CONFIG_EP after aborting leftover Invalid (CC=0). `xhci cmdptr pN` is this EFI matching Command Completion by Command TRB Pointer (xHCI 6.4.2.2) so p10 abort CC=0 cannot retire p11 CONFIG_EP. `xhci toshiba pN named` means Toshiba was addressed then CONFIG_EP still failed — not "device missing". `xhci p11first` is Toshiba-first port walk (11..=max then 1..=10). `xhci nop` is a No-Op Command after HCRST so Enable Slot is not the lost first doorbell. `xhci slotretry pN` is Enable Slot retry on the same port after abort (do not walk to the hub). `xhci descabort pN keep-slot` is abort + retry EP0 GET_DESC on the live slot, then port-reset + Reset Device (`xhci rstdev`) + Address Device on the **same** slot (not Enable Slot, not Disable Slot, not p14). `xhci stopwalk pN keep-slot` means a named MSC (Toshiba) BOT/enum fail kept DCBAA and did **not** walk p14/p10. `cmd=3 cmpl=0x13` after port-reset Address Device is Context State Error on an already-Addressed slot — Reset Device first. `cmd=4 cmpl=0xff` with `ep0 timeout (not Evaluate Context)` is GET_DESC with no Transfer Event; Evaluate Context is `xhci eval` / packed cmd=7. `xhci abort crr=` is command-ring abort after Address Device timeout (crr=0 cleared, crr=1 still running). `xhci firstread pN overlap` is after CAPACITY, overlapping the required 512-byte READ. `xhci firstcbw pN norearm` was long wait with no Stop+rearm. `uas-no-bot` means MSC protocol 0x62 only (BOT 0x50 missing). `hub skip` / `err=9` is USB class 09 (not BOT) and must **not** clobber a prior BOT `cmpl`. GET_DESC retries print `desc retry n=`. `usb rw ok` prints first 8 + first WRITE + every 4096 (`xhci_rw_ok_should_print`); `usb rw fail` stays n<8 or every 64. `xhci hcrst` means the controller was halted and rebuilt on our rings (not UEFI BlockIo).

Fail: `nvme I/O fail` / `usb I/O fail err=` + leftover DRAM. Iron guest8g COM2 (2026-09-18, EFI `b661808c`): leftover DRAM **skipped**. Front USB 2 UDisk; `xhci p11first` + `xhci nop` then Toshiba `0480:a004` named; `eval mps=64`; BOT `08/06/50`; **`xhci setcfg p11 val=1` first try**; CONFIG_EP `epst ep0=1 out=1 in=1`; `botrst fail` continue; `maxlun skip`; `firstcbw overlap`; `cswqueue`; `capoverlap`; `firstread overlap`; `usb I/O ready bytes=320072933376 lba=512`; leftover skip persist; peek `efi=EFI PART gpt=0 fit=0 gpt_err=0 usb_err=0 lba=512 guest=8589934592 bootx64=0 ext4=0 installed=0`; virtio `bytes=8589934592 keep=0 (durable LUN usb)` (**8 GiB virtio**). Coexist `HOST-NIC-HTTP-OK` `10.99.99.146:8443` → SPA Start → Alpine `[vda] 16777216 512-byte logical blocks (8.59 GB/8.00 GiB)`; `GPT PMBR size mismatch (625142447 != 16777215) will be corrected by write`; first `wr=1` at `off=0x1ffffb000` then `RAYNU-V-M7-ISO-INSTALL-OK` on the **8 GiB** USB window; setup-disk (`Installing system on /dev/vda2` / `Installing for x86_64-efi platform` / `Installation finished. No error reported.`) → **`Installation is complete. Please reboot.`** → guest `reboot` → `guest reset requested src=kbc n=1` → F7 `image=DISK-BOOTX64` → GNU GRUB 2.12 → `RAYNU-V-RAYNU-F-DISK-BOOT-OK` → second Linux `root=UUID=348005a9-25b7-4027-bc95-af9aef244c3a` on 8 GiB `vda` (`vda: vda1 vda2`; `EXT4-fs (vda2): mounted filesystem 348005a9-…`; fsck `vda2` 6377/521216 + `vda1` 5 files) → `login: root`. After login `usb rw ok wr=1` `n` 1.16 M `off=0x184f72…` ≈ 6.08 GiB / ~76% of 8 GiB — **ext4lazyinit**, not a hang. iDRAC then `Connection to 10.99.99.24 closed by remote host` at `n=1163264`; reconnect is blank at `localhost:~#` (smash Enter; curl coexist `10.99.99.146:8443`). UART mash recovered. Early `usb rw fail off=0x1600 wr=0 err=8 cmpl=0x00010300 bot=cbw scsi=read` then oks. **Not persist.** USB F7 `DISK-BOOT-OK` ≠ leftover DRAM Everest ≠ Force Off `keep=1`. Guest F7 ≠ persist. Iron guest8g A2 COM2 (same EFI `b661808c` after HV-not-up): leftover skip; peek `efi=EFI PART gpt=1 fit=1 gpt_err=0 usb_err=0 guest=8589934592 bootx64=1 ext4=1 installed=1`; virtio `bytes=8589934592 keep=1 (durable LUN usb)`. UART mash recovered. Coexist `HOST-NIC-HTTP-OK` `10.99.99.146:8443` → SPA Start → `E4 SPA start — RayNu-F product ISO` → `FAT ESP mounted lba=125` → `BOOTX64.EFI bytes=724992` → **`image=ISO-BOOTX64`** (not `DISK-BOOTX64` 139264). Linux `modules=loop,squashfs` (ISO live, not `root=UUID=348005a9-…`). Then `usb rw fail off=0x800 wr=0 err=8 cmpl=0xff bot=data scsi=read`; Alpine `[vda] 8.00 GiB` then `I/O error, dev vda, sector 0` / `unable to read partition table`; ISO `vdb` mounted; RCU stall / `blkid` in `iowrite16`. **Not persist.** Keep=1 attach lived; RayNu-F GPT array CRC + cold BOT after idle lost DISK-BOOT. This EFI: skip array CRC + sticky keep + `xhci diskprime` (INQUIRY+LBA0) before stage. **Force Off** the hung ISO. Do **not** `setup-disk` (would wipe if USB writes). Do not boot Dell `EFI Fixed Disk`. Do not flash Toshiba. Iron guest8g skip-CRC EFI COM2 (`4af78b43`) A2 **CLOSED on evidence:** leftover skip; peek `efi=EFI PART gpt=1 fit=1 gpt_err=0 usb_err=0 guest=8589934592 bootx64=1 ext4=1 installed=1`; virtio `bytes=8589934592 keep=1 (durable LUN usb)`. Coexist `HOST-NIC-HTTP-OK` `10.99.99.146:8443` → SPA Start → `xhci diskprime` → `GPT ESP lba=2048` `BOOTX64.EFI bytes=139264` `image=DISK-BOOTX64` → GNU GRUB 2.12 → `RAYNU-V-RAYNU-F-DISK-BOOT-OK` → Linux `root=UUID=348005a9-25b7-4027-bc95-af9aef244c3a` on `[vda] 8.00 GiB` `vda1 vda2` → `EXT4-fs (vda2): mounted filesystem 348005a9-…` → `login: root`. Minted `RAYNU-V-M8-DISK-PERSIST-OK` **did not print**. Spurious `ISO-INSTALL-OK` on journal recovery `wr=1`. Auto-answer `setup-disk` then `No disks found` (did not wipe). Sit at `localhost:~#`. Do **not** `setup-disk`. Do not Force Off. Do not boot Dell `EFI Fixed Disk`. Do not flash Toshiba. Iron setcfgretry COM2 (2026-09-18, EFI `ef7e93ec`): leftover DRAM **skipped**. `xhci p11first` + `xhci nop` then Toshiba `0480:a004` named; `eval mps=64`; BOT `08/06/50`; **`xhci setcfg p11 val=1` first try** (no `xhci setcfgretry` line); CONFIG_EP `epst ep0=1 out=1 in=1`; `botrst`; `maxlun skip`; `firstcbw overlap`; `cswqueue`; `capoverlap`; `firstread overlap`; `usb I/O ready bytes=320072933376 lba=512`; leftover skip persist; peek `efi=EFI PART gpt=1 gpt_err=0 usb_err=8 bootx64=1 ext4=0 installed=0` (partial writequeue GPT, incomplete grub — F7 `no \EFI\BOOT\BOOTX64.EFI` then ISO-BOOTX64). Virtio `bytes=320072933376 keep=0 (durable LUN usb)`. Coexist `HOST-NIC-HTTP-OK` → SPA Start → Alpine `[vda] 625142448 (320 GB/298 GiB)` `vda: vda1 vda2`; first `wr=1` at `off=0x4a85d51000` n=12798 then `RAYNU-V-M7-ISO-INSTALL-OK` on the 298 GiB USB LUN; UART mash (`virtio MMIO gpa=` overlay + `usb rw ok` every 4096) recovered through setup-disk (`Installing system on /dev/vda2` / `Installing for x86_64-efi platform` / `Installation finished. No error reported.` / initramfs-lts / grub-mkconfig) → **`Installation is complete. Please reboot.`** → guest `reboot` → `guest reset requested src=kbc n=1` → F7 relaunch → `found \EFI\BOOT\BOOTX64.EFI bytes=139264 (F7 disk)` → `image=DISK-BOOTX64` → GNU GRUB 2.12 `*Alpine Linux v3.21, with Linux lts` 2s auto (`error: no suitable video mode` / `unable to determine partition UUID` are harmless). GRUB 2s→1s→0s → `RAYNU-V-RAYNU-F-DISK-BOOT-OK` → second Linux `root=UUID=6d549fd4-3907-4382-a3eb-750ee9d52616` on 298 GiB `vda` (625142448) → `EXT4-fs (vda2): mounted filesystem 6d549fd4-…` → `fsck` `vda2` 6377/19537920 + `vda1` 5 files → `login: root` → `cat /proc/cmdline`. After login COM2 keeps `usb rw ok wr=1` every 4096 (`n` past 3.1 M) plus virtio MMIO overlay — **not a hang**; USB WRITEs are succeeding. Hours later still `usb rw ok wr=1` (`n` 3.1 M → 10.7 M, `off=0x3b03…`/`0x3b84…` ≈ 252 GB / ~79% of the 320 GB LUN). Payload ≈ 7.6 M × 512 B ≈ 3.9 GiB vs ~245 GiB LBA walk — **ext4lazyinit** on freshly mkfs'd 298 GiB `vda2` after remount-rw, not a hang, not a second `setup-disk`. **Not persist.** Guest F7 reboot-to-disk on USB LUN ≠ Force Off persist ≠ leftover DRAM Everest ≠ `RAYNU-V-M8-DISK-PERSIST-OK`. Do not Force Off (dirty ext4). Do not reflash while lazyinit is writing. Iron Force Off COM2 (same EFI `ef7e93ec`): leftover DRAM **skipped**. Toshiba `0480:a004` named; `xhci setcfg p11 val=1`; `usb I/O ready bytes=320072933376`; leftover skip persist; peek `efi=3?????|? gpt=0 gpt_err=2 usb_err=0 lba=512 bootx64=0 ext4=0 installed=0`; virtio `keep=0 (durable LUN usb)`. Coexist `HOST-NIC-HTTP-OK` `10.99.99.146:8443` Phase B idle (no SPA Start). Guest F7 `DISK-BOOT-OK` / `root=UUID=6d549fd4-…` did **not** survive HV reboot. **Not persist.** `gpt_err=2` is `NoProtectiveMbr`. Dell `EFI Fixed Disk` on back USB is firmware NVRAM, not `keep=1`. Do not SPA Start (SETUP would wipe). Do not reflash. SET_CONFIG first-try falsifies “okquiet leftover is every boot.” Retry path unused this boot; keep `xhci setcfgretry`. Keep writequeue / okquiet / CSW queue / nopretry / addrretry / Bot-only SENSE / nlb=1 / stopwalk. Iron okquiet COM2 (2026-09-18, EFI `fba9bc49`): leftover DRAM **1.07 GiB**. `xhci p11first` + `xhci nop` then Toshiba `0480:a004` named; `eval mps=64`; BOT `08/06/50`; then `desc retry n=1,n=2` (SET_CONFIG `control_nodata_retry`); abort crr=0; `xhci stopwalk p11 keep-slot`; `xhci enum p11 cmd=6 cmpl=0xff timeout` `bot=? scsi=?`. Packed fail `cmpl=0x000306ff` `portsc=0x0b00000e03` is p11 SET_CONFIG timeout. **No** `xhci setcfg val=1`. **No** `usb I/O ready 298GiB`. Virtio leftover `keep=0` `[vda] 1.07 GiB`; coexist `HOST-NIC-HTTP-OK` → SPA Start → leftover Alpine `setup-disk` → `ISO-INSTALL-OK` → F7 `DISK-BOOT-OK` → login `root=UUID=53bb2054-…`; stall dump leftover `last_st=0x0`. **Leftover DRAM Everest ≠ persist.** Okquiet print change cannot cause SET_CONFIG timeout (bring-up is before `usb rw ok`). Writequeue COM2 already wrote a partial Alpine onto the 298 GiB Toshiba; this boot did not touch that LUN. Abort, re-prime No-Op, reset EP0, retry SET_CONFIGURATION (`xhci setcfgretry`). Keep writequeue / okquiet / CSW queue / nopretry / addrretry / Bot-only SENSE / nlb=1 / stopwalk. Do not Force Off leftover guest. Do not revert okquiet. setcfgretry COM2 is this EFI. Iron addrretry COM2 (2026-09-17, EFI `116dbb1f`): leftover DRAM **skipped**. `xhci p11first` + `xhci nop` timeout + abort + `xhci nopretry` + Enable Slot abort + `xhci slotretry p11` then Toshiba `0480:a004` named; `eval mps=64`; BOT `08/06/50`; `setcfg val=1`; CONFIG_EP `epst ep0=1 out=1 in=1`; `botrst`; `maxlun skip`; `firstcbw overlap`; **`xhci cswqueue p11`**; `capoverlap`; `firstread overlap`; `usb I/O ready bytes=320072933376 lba=512`; leftover skip; peek `efi=????????` `gpt_err=2` `usb_err=8` (latched Xfer, not `read-fail`; `gpt_err=2` is NoProtectiveMbr on a blank LUN). **No** leftover 1.07 GiB. **No** p14. Guest Alpine paste (same EFI `116dbb1f`): coexist `HOST-NIC-HTTP-OK` → SPA Start → Alpine `[vda] 625142448 512-byte logical blocks (320 GB/298 GiB)` `vda: vda1`; ISO `vdb` FAT; login; many `usb rw ok` READs including high LBA `0x4a85…`. Auto-answer `setup-disk` then WRITE CBW timeout `usb rw fail off=0x4a85d51200 wr=1 err=8 cmpl=0xff bot=cbw scsi=write` (backup GPT); later `cmpl=0x00020404`; READ CBW fail at `off=0`; `sfdisk: cannot open /dev/vda: I/O error` + soft lockup `[sfdisk:2171]`. **Not persist.** Queue WRITE DATA OUT + CSW IN (`xhci writequeue`) + extra settle before first WRITE (`xhci writesettle`). Keep CSW-queued READ / nopretry / addrretry / Bot-only SENSE / nlb=1 / stopwalk. Do not Force Off. Do not treat partial GPT writes as persist. Iron writequeue COM2 (2026-09-18, EFI `f32b9238`): leftover DRAM **skipped**. Toshiba `0480:a004` named; `xhci cswqueue p11`; `usb I/O ready bytes=320072933376`; leftover skip; peek `efi=????????` `gpt_err=2` `usb_err=0`. Coexist `HOST-NIC-HTTP-OK` → SPA Start → Alpine `[vda] 298 GiB` `vda1` login; first `wr=1` at `off=0x4a85d54a00` then `RAYNU-V-M7-ISO-INSTALL-OK` on the 298 GiB USB LUN (not leftover DRAM Everest); then `usb rw ok wr=1` every 64 I/Os mashed COM2 during setup-disk (`Partition #1 contains a exfat signature`). **Not persist.** Do not Force Off mid-install. USB `ISO-INSTALL-OK` ≠ `RAYNU-V-M8-DISK-PERSIST-OK`. This EFI: quiet `usb rw ok` (`xhci_rw_ok_should_print` first 8 + first WRITE + every 4096; keep fail prints). Keep writequeue / CSW queue / nopretry / addrretry / Bot-only SENSE / nlb=1 / stopwalk. okquiet COM2 is this EFI. Iron cswsettle COM2 (2026-09-17, EFI `5c51c4ea`): leftover DRAM **1.07 GiB**. `xhci p11first` + `xhci nop` then abort crr=0; p11 ADDR `cmd=3 cmpl=0xff`; p14 hub skip + p10 ADDR timeout; `usb I/O fail err=3`; virtio leftover `keep=0`; coexist `HOST-NIC-HTTP-OK` → SPA Start → leftover Alpine `setup-disk` → `ISO-INSTALL-OK` → F7 `DISK-BOOT-OK` → login `root=UUID=d43dd928-…`; stall dump leftover `last_st=0x0`. **No** Toshiba `0480:a004`. **No** `xhci cswqueue`. **Leftover DRAM Everest ≠ persist.** Re-prime No-Op after abort (`xhci nopretry`) + retry Address Device (`xhci addrretry`). Keep CSW queue / Bot-only SENSE / nlb=1 / stopwalk. Iron sensebot COM2 (2026-09-17, EFI `c0a726de`): leftover DRAM **skipped**. Toshiba `0480:a004` named; no p14; `usb I/O ready bytes=320072933376`; peek `efi=????????` `gpt_err=2` `usb_err=8` (latched CAPACITY Xfer; not `read-fail`); virtio `keep=0`; coexist `HOST-NIC-HTTP-OK`; Alpine `[vda] 298 GiB`; nlb=1 `usb rw ok` at `0x0`/`0x200`/`0x400`/`0x600`/`0x800` then `bot=csw scsi=read cmpl=0xff`; `vda: vda1`; ISO `vdb` FAT lived; later CSW timeouts + RCU/soft lockup in `nlplug-findfs`/`vp_notify`. **No** `scsi=sense`. **Not persist.** Queue CSW IN with DATA (`xhci cswqueue`) + settle between nlb=1 chunks; keep Bot-only SENSE / `usb_bot_guest_chunk`. Keep sensebot/nlb1/stopwalk/rstdev/stallquiet/FIRST_READ_SPINS. Do not Force Off. Do not setup-disk until `vda` stays `last_st=0x0`. Iron nlb1 COM2 (2026-09-17, EFI `920f606b`): leftover DRAM **skipped**. Toshiba `0480:a004` named; no p14; `usb I/O ready bytes=320072933376`; peek `usb_err=0` `gpt_err=2`; virtio `keep=0`; coexist `HOST-NIC-HTTP-OK`; Alpine `[vda] 298 GiB`; nlb=1 `usb rw ok` at `0x0`/`0x200`/`0x400`/`0x600`/`0x800` then `bot=cbw scsi=sense` / `bot=data scsi=read` / `bot=csw scsi=sense`; `vda: unable to read partition table`. ISO `vdb` FAT lived. **Not persist.** REQUEST SENSE only on `UsbBotError::Bot` (CSW status != 0), never on Xfer timeout; keep `usb_bot_guest_chunk`. Keep nlb1/stopwalk/rstdev/stallquiet/FIRST_READ_SPINS. Do not Force Off. Do not setup-disk until `vda` stays `last_st=0x0`. Iron stopwalk COM2 (2026-09-17, EFI `90af2c5b`): leftover DRAM **skipped**. Toshiba `0480:a004` named; no p14; `usb I/O ready bytes=320072933376`; virtio `keep=0`; Alpine `[vda] 298 GiB` `login: root`; peek `gpt_err=2`; guest `vda last_st=0x1`; `sfdisk: cannot open /dev/vda: I/O error`. ISO `vdb last_st=0x0`. **Not persist.** One native LBA per BOT (`usb_bot_guest_chunk`); REQUEST SENSE after any CSW-stage fail stacked on Xfer timeout (nlb1). Keep stopwalk/rstdev/stallquiet/FIRST_READ_SPINS. Do not Force Off. Do not setup-disk until `vda` stays `last_st=0x0`. Iron rstdev/udiskkick COM2 (2026-09-17, EFI `9f251ff6`): leftover DRAM **1 GiB**. `xhci p11first` + `xhci nop` + p11 Toshiba `0480:a004` named; `eval mps=64`; BOT `08/06/50`; `setcfg val=1`; CONFIG_EP `epst ep0=1 out=1 in=1`; `botrst fail` continue; `maxlun skip`; `firstcbw overlap`; `usb rw wait` ×2 (`bot=csw scsi=inquiry`); abort then p14 hub `1604:10c0` skip + p10 ADDR `cmd=3 cmpl=0xff`. Packed fail `cmpl=0x303ff` `portsc=0xa00000e03` is **p10**, not the live p11 slot. **Regression vs guestio (`f2c55be4`) `usb I/O ready`.** `xhci_desc_fail_stops_walk` is GET_DESC-only. **Leftover DRAM ≠ persist.** This EFI: `xhci stopwalk` after named MSC; keep DCBAA; abort + recover_pipes + retry BOT on the live slot; no Disable Slot; no p14/p10. Keep rstdev/stallquiet/guestio/descabort/nop/slotretry/p11first. Do not Force Off. Iron stallquiet COM2 (2026-09-17): leftover DRAM **1 GiB**. `xhci p11first` + `xhci nop` + p11 GET_DESC retry n=1,n=2 then `cmd=4 cmpl=0xff` `ep0 timeout`; `xhci descabort p11 keep-slot`; second GET_DESC timeout; then `cmd=3 cmpl=0x13` + `cmd=4 cmpl=0x13` `err=3 bot=? scsi=?`. Toshiba **not named**. Port-reset + Address Device on an already-Addressed slot is Context State Error. **Leftover DRAM ≠ persist.** This EFI: Reset Device (`xhci rstdev`) before re-Address; keep DCBAA; no Disable Slot; no p14 walk. Keep stallquiet/guestio/descabort/nop/slotretry/p11first. Do not Force Off. Iron guestio COM2 (2026-09-16, EFI `f2c55be4`): leftover DRAM **skipped**. Toshiba `0480:a004` named; `usb I/O ready bytes=320072933376`; virtio `keep=0`. Alpine `[vda] 298 GiB` `vda1`; stall dump `disk_last=3 iso_last=556 disk_used=3 iso_used=556` during ISO mount (leftover apk overlay, queues drained). **No** `usb rw fail`. `eval mps=64 fail` then BOT still ready. **Not persist.** This EFI: hold stall dump until `Installing packages` while USB LUN live; print `usb rw ok` / `usb rw wait`. Keep FIRST_READ_SPINS/descabort. Do not Force Off. Iron descabort COM2 (2026-09-16, EFI `b0c2b678`): leftover DRAM **skipped**. Toshiba `0480:a004` named; `usb I/O ready bytes=320072933376`; peek `usb_err=0` `gpt_err=2`; virtio `bytes=320072933376 keep=0 (durable LUN usb)`. Alpine `[vda] 298 GiB` then mixed `last_st=0x0` / `last_st=0x1`; `sfdisk: cannot open /dev/vda: I/O error`. ISO `vdb` `last_st=0x0` `Mounting boot media: ok.` `login: root`. Guest BOT after bring-up used `BULK_SPINS`; first READ used `FIRST_READ_SPINS`. **Not persist.** This EFI: keep `FIRST_READ_SPINS` for live guest BOT; print `usb rw fail off= bot= scsi=` (rate-limited). Keep descabort/nop/slotretry/p11first/cmdptr. Do not Force Off. Do not setup-disk until `vda` stays `last_st=0x0`. Iron slotretry COM2 (2026-09-16): leftover DRAM. `xhci p11first` then `xhci nop` then p11 GET_DESC retry n=1,n=2 then `cmd=4 cmpl=0xff timeout` — Toshiba **not named**. Enable Slot and Address Device lived (no `cmd=2` / `slotretry`). abort crr=0; p14 hub `1604:10c0` GET_DESC succeeded. Packed fail `cmpl=0x303ff` `portsc=0xa00000e03` is p10 ADDR, not the p11 GET_DESC. Stop+DESC-retry did not recover EP0; abort then a new slot's GET_DESC did. **Leftover DRAM ≠ persist.** This EFI: keep the live slot (`xhci descabort pN keep-slot`); abort without Disable Slot; retry EP0 GET_DESC then port-reset + Address Device on the same slot; do not start p14/p10; fail durable-LUN. Packed `cmd=4 cmpl=0xff` is EP0 GET_DESC (no Transfer Event), not Evaluate Context (`xhci eval` / cmd=7). Keep nop/slotretry/p11first/cmdptr. Do not Force Off leftover guest. Never treat leftover 1 GiB as the disk. Iron p11first COM2 (2026-09-16): leftover DRAM. `xhci p11first` then `xhci abort crr=0` then p11 Enable Slot `cmd=2 cmpl=0xff timeout` — Toshiba **not named**. p14 hub `1604:10c0` skip then p10 ADDR `cmd=3 cmpl=0xff`. Packed fail `cmpl=0x303ff` `portsc=0xa00000e03` is p10 ADDR. First Enable Slot after HCRST lost; abort primed the ring; product walked to the hub. **Leftover DRAM ≠ persist.** This EFI: No-Op prime (`xhci nop`); Enable Slot ADDR_SPINS; abort+retry same port (`xhci slotretry pN`); accept Command TRB Pointer 0 on Success. Keep p11first + cmdptr. Do not Force Off leftover guest. Iron cmdptr COM2 (2026-09-16): leftover DRAM. p10 Address Device `cmd=3 cmpl=0xff` then p11 Enable Slot `cmd=2 cmpl=0xff` then p14 Address Device `cmd=3 cmpl=0xff`. **No** `vid=` — Toshiba not named. Packed fail `cmpl=0x303ff` `portsc=0xe00000e03` is p14 ADDR timeout. Command-pointer match skipped Command Ring Stopped from p10 abort; Disable Slot wait wedged CRR. Prior capoverlap boot named `0480:a004` on p11. **Leftover DRAM ≠ persist.** This EFI: walk p11 before p10 (`xhci p11first`); abort waits ADDR_SPINS; no Disable Slot wait (`xhci abort crr=`). Keep cmdptr. Do not Force Off leftover guest. Iron capoverlap COM2 (2026-09-16): leftover DRAM. p11 Toshiba `0480:a004` **was named** (`vid=`/`eval`/`bot`/`setcfg`) then `xhci enum p11 cmd=5 cmpl=0` (`err=3 bot=? scsi=?` packed `cmpl=0x30500` = CC=0). Not "device missing". p10 Address Device timeout leftover Invalid retired CONFIG_EP; recover_enum Disable Slot'd Toshiba; mapper printed need-media. Recover/DESC-retry `usb I/O ready bytes=320072933376` is the lived I/O-ready. **Leftover DRAM ≠ persist.** Lived successor: match Command Completion by TRB pointer (`xhci cmdptr`); keep drain/skip-CC=0/retry. Packed `portsc=0xb00000e03` is p11. SNP/HTTP timeout is not this bug. p14 hub skip is not this bug. Do not Force Off leftover guest. Iron capoverlap COM2 (2026-09-16): leftover DRAM. p11 Toshiba `0480:a004` + `xhci setcfg p11 val=1` then `xhci enum p11 cmd=5 cmpl=0` (`err=3 bot=? scsi=?` packed `cmpl=0x30500` = CC=0 + cmd=5 + speed=3). BOT never started — leftover Invalid command event retired CONFIG_EP. Packed `portsc=0xb00000e03` is p11. SNP/HTTP timeout is not this bug. p10 Address Device timeout and p14 hub skip are not this bug. **Leftover DRAM ≠ persist.** Lived successor: drain events, skip CC=0, retry Configure Endpoint once (`xhci config` / `xhci cfgretry`) — leftover Success/Invalid still matched any command event. Do not Force Off leftover guest. Intel PCH scratchpad ≤ 64 pages; more is `err=1` Cap + `over-budget`. `mmio-dead` means CAPLENGTH was 0 or `0xffffffff` (BAR unread). Iron overlap (2026-09-16): leftover DRAM. p11 Toshiba `0480:a004` + `xhci maxlun p11 skip` + firstcbw `epst ep0=1` + `xhci firstcbw p11 overlap` then `usb I/O fail err=8 bot=cbw scsi=capacity cmpl=0xff` leftover 1 GiB. INQUIRY overlap lived; sequential CAPACITY CBW timed out. **Leftover DRAM ≠ persist.** Lived successor: overlap CAPACITY + READ IN (`xhci capoverlap`) — this boot never reached BOT. Do not Force Off leftover guest. Iron skipmaxlun (2026-09-16): leftover DRAM. p11 Toshiba `0480:a004` + CONFIG_EP `epst ep0=1 out=1 in=1` + `botrst` + `xhci maxlun p11 skip` then firstcbw `epst ep0=1` + `xhci firstcbw p11 norearm`. INQUIRY CBW `cmpl=0xff` leftover 1 GiB. EP0 stayed Running — DATA-then-Stop is falsified. **Leftover DRAM ≠ persist.** This EFI: overlap first INQUIRY (`xhci firstcbw pN overlap`) — arm CBW OUT and DATA IN before waiting. Do not Force Off leftover guest. Iron norearm (2026-09-16): leftover DRAM. p11 Toshiba `0480:a004` + CONFIG_EP `epst ep0=1 out=1 in=1` + `botrst` + GET_MAX_LUN `val=0` then `epst ep0=3 out=1 in=1` before `xhci firstcbw p11 norearm`. INQUIRY CBW `cmpl=0xff` leftover 1 GiB. GET_MAX_LUN DATA-then-Stop left EP0 Stopped. **Leftover DRAM ≠ persist.** This EFI: skip optional GET_MAX_LUN (`xhci maxlun pN skip`) so EP0 stays Running before first CBW. Do not Force Off leftover guest. Iron firstcbw (2026-09-16): leftover DRAM. p11 Toshiba `0480:a004` + `xhci epst` Running + `botrst` + `maxlun val=0` + `xhci firstcbw p11` then `usb I/O fail err=8 bot=cbw scsi=inquiry cmpl=0xff` `portsc=…0b00000e03` (p11). FIRST_READ_SPINS + Stop+rearm did not retire the CBW. **Leftover DRAM ≠ persist.** This EFI: `xhci firstcbw norearm` — long wait, keep CONFIG_EP dequeue (Set TR Deq is fire-and-forget). Do not Force Off leftover guest. Iron firstread (2026-09-16): leftover DRAM. p11 Toshiba `0480:a004` + `xhci epst p11 ep0=1 out=1 in=1` (Running) + `xhci botrst` + `maxlun val=0` then `usb I/O fail err=8 bot=cbw scsi=inquiry cmpl=0xff` `portsc=…0b00000e03` (p11). **No** `xhci firstread` — that hook ran after CAPACITY. First bulk OUT timed out. **Leftover DRAM ≠ persist.** Lived successor: `prepare_first_cbw` (`xhci firstcbw`) before INQUIRY — firstcbw COM2 still `scsi=inquiry`. Do not Force Off leftover guest. Iron epst (2026-09-16): leftover DRAM. p11 Toshiba `0480:a004` + `xhci eval mps=64` + `xhci bot ep_out=2 ep_in=1` + `setcfg val=1` + `xhci epst p11 ep0=1 out=1 in=1` (Running) + `xhci botrst` + `maxlun val=0` then INQUIRY/CAPACITY lived and `usb I/O fail err=8 bot=cbw scsi=read cmpl=0xff` `portsc=…0b00000e03` (p11). CONFIG_EP packing is Running. First 512-byte READ CBW timed out. **Leftover DRAM ≠ persist.** Lived successor: `prepare_first_read` (`xhci firstread`) after CAPACITY — this boot never got there. Do not Force Off leftover guest. Iron maxlun (2026-09-16): leftover DRAM. p11 Toshiba `0480:a004` + `xhci eval mps=64` + `xhci bot ep_out=2 ep_in=1` + `setcfg val=1` + `xhci maxlun p11 val=0` then `usb I/O fail err=8 bot=cbw scsi=inquiry cmpl=0xff` `portsc=…0b00000e03` (p11). Enum/GET_CONFIG/SET_CONFIG/GET_MAX_LUN lived. First bulk OUT timed out. **Leftover DRAM ≠ persist.** Lived successor: CONFIG_EP copies Output Slot+A1 live EP0 (`xhci epst`); MSC BOT reset + Clear Halt (`xhci botrst`) before GET_MAX_LUN/INQUIRY — INQUIRY/CAPACITY then lived on epst COM2. Do not Force Off leftover guest. Iron ep0-eval (2026-09-15): leftover DRAM. p11 Toshiba `0480:a004` + `xhci eval mps=64` + `xhci bot ep_out=2 ep_in=1` + `setcfg val=1` then `usb I/O fail err=8 bot=cbw scsi=capacity cmpl=0` `portsc=…0b00000e03` (p11). Enum/GET_CONFIG/SET_CONFIG lived. `cmpl=0` is p14 Disable Slot clobber. **Leftover DRAM ≠ persist.** This EFI: GET_MAX_LUN after CONFIG_EP (`xhci maxlun`); required INQUIRY; 80M settle; recover_enum holds BOT `cmpl`. Do not Force Off leftover guest. Iron cfg-desc (2026-09-15, EFI `b5338458`): leftover DRAM. p11/p14 GET_DEVICE retry `n=1,n=2` then `cmd=4 cmpl=0xff` — **no** `vid=` (CH on SETUP+DATA broke Lewisburg EP0). p14 fail `portsc=0xe00000e03`. **Leftover DRAM ≠ persist.** Iron ep0-stop (2026-09-15): leftover DRAM. p11 Toshiba `0480:a004` device desc lived (`desc retry n=1`) then 9-byte config GET_DESC `cmd=4 cmpl=0xff` `bot=? scsi=?` `cmpl=0x304ff`. A CH revert returns here. This EFI: unchained control TDs; DATA completes immediately then Stop leftover STATUS; Evaluate Context EP0 MPS (`xhci eval`); one-packet GET_CONFIG 64; Toshiba `0480:a004` BOT fallback (`xhci cfg-skip`, `ep_out=2 ep_in=1` from `96024edc`). Do not Force Off leftover guest. Iron stop-ep (2026-09-15): leftover DRAM. p11 Toshiba `0480:a004` device desc lived (`desc retry n=1`) then config GET_DESC `cmd=4 cmpl=0xff` `bot=? scsi=?` `cmpl=0x304ff`. BOT never started — timeout retries stacked EP0 TRBs (Reset EP0 skipped on timeout). **Leftover DRAM ≠ persist.** This EFI: Stop Endpoint on EP0 GET_DESC timeout, then retry. Do not Force Off leftover guest. Iron first-cbw (2026-09-15): leftover DRAM. p11 Toshiba `0480:a004` SET_CONFIG held then `usb I/O fail err=8 cmpl=0x13 bot=cbw scsi=capacity`. `cmpl=0x13` is Context State Error — Reset Endpoint on a **Running** bulk EP after CBW timeout (xHCI 4.6.8 is Halted-only). **Leftover DRAM ≠ persist.** This EFI: Stop Endpoint then Set TR Dequeue; Reset Endpoint only if Stop fails; recover commands do not clobber BOT `cmpl`. Do not Force Off leftover guest. Iron cap-csw (2026-09-15, skip-INQUIRY EFI): leftover DRAM. p11 Toshiba `0480:a004` SET_CONFIG held then `usb I/O fail err=8 cmpl=0xff bot=cbw scsi=capacity`. First bulk OUT after SET_CONFIG timed out — skipped/ignored INQUIRY was the HDD warmup + delay. **Leftover DRAM ≠ persist.** This EFI: settle + waited INQUIRY (`recover_pipes` on fail, never `let _ =`); then CAPACITY retry. Do not Force Off leftover guest. Iron `0780df21` (2026-09-15): leftover Everest (`keep=0` `vda` 1.07 GiB `root=UUID=1bc8b57d-…`). Coexist `HOST-NIC-HTTP-OK` `10.99.99.145:8443` → SPA Start → `ISO-INSTALL-OK` → F7 `DISK-BOOT-OK` → login. USB p11 Toshiba `0480:a004` SET_CONFIG held then `usb I/O fail err=8 cmpl=0xff bot=csw scsi=capacity` (CAPACITY CSW timed out — ignored INQUIRY can leave a pending IN TRB). **Leftover DRAM Everest ≠ persist.** Do not Force Off leftover guest. Iron `6ba076cc` (2026-09-15): leftover Everest (`keep=0` `vda` 1.07 GiB). p11 Toshiba `0480:a004` `xhci bot p11 iface=08/06/50 ep_out=2 ep_in=1` + `xhci setcfg p11 val=1` then `usb I/O fail err=8 bar=… portsc=…0b00000e03 cmpl=0xff bot=cbw scsi=read` (CAPACITY ok; native READ CBW timed out — leftover events no longer false-complete). SNP DHCP failed (`SNP DHCP failed (no lease)`) → `no parked SNP` → analog/coexist skip → Phase B idle `SPA Start needs HTTP`. Native BCM5720 DHCP (`HOST-NIC BCM5720 DHCP discover` / `DHCP lease` / `CURL NOW` / `coexist listening`); analog always post-EBS even without SNP lease; CBW timeout `cmpl=0xff` `recover_pipes` so retries do not stack TRBs. Iron `cbd9bf47` (2026-09-15): p10 Address Device `cmd=3 cmpl=0xff timeout`; p11 Toshiba `0480:a004` device desc then config GET_DESC `cmd=4 cmpl=0xff` (`bot=? scsi=?`); leftover Everest `vda` 1.07 GiB `root=UUID=35bfa02f-…` — **not persist**. Wait GET_DESC STATUS TRB (not any Transfer Event); `DESC_SPINS`; do not Reset EP0 on timeout. Iron `ac3b92cd` (2026-09-14): Lewisburg `8086:a1af` named, then `usb I/O fail err=1 bar=0x92b00000 portsc=0 cmpl=0` — Cap before PORTSC; 16-page budget. Cap EFI `67db8a68` (`build: sha=50b5d8bb29fb`): `scratch=34/64` then Polling `0x000206e1`. Enum EFI `53d1f7f7` (`build: sha=c6bdd671b330`): HS U0 `sc=0x00000e03` PED=1 speed=3 then `xhci enum p10/p11/p14 cmd=3 cmpl=0x11` — Address Device Parameter Error (Slot Context DW1 Number of Ports was the port number; Hub=0). Slot DW1 EFI `3473a0b9`: Parameter Error gone; p10 Address Device then p11/p14 Enable Slot printed `cmpl=0x40` (stale MaxSlots after Address Device never posted; command ring stayed busy so later CCS ports never got a fair try). Guest `vda` was 1.07 GiB leftover DRAM. Toshiba unused. Everest loop (`ISO-INSTALL-OK` → F7 `DISK-BOOT-OK` → `login:`) is **not** persist. Do not Force Off that guest expecting `RAYNU-V-M8-DISK-PERSIST-OK`. Do not setup-disk on leftover. Recover EFI `ba0ab8ab` / COM2 `build: sha=568e869632d6`: p10 Address Device `cmpl=0xff timeout`, p11 Toshiba `0480:a004` `usb I/O ready bytes=320072933376`, leftover skipped, peek `efi=read-fail usb_err=3`, Alpine `I/O error` on **both** `vda` and `vdb` (`last_st=0x1`). Enum recover is proven; BOT READ + ISO isolation were not. Isolation EFI `06ca0f95` / COM2 `06ca0f952d69`: product ISO virtio never uses DurableLun — Alpine `Mounting boot media: ok.` `last_st=0x0`, leftover `setup-disk` → `ISO-INSTALL-OK` → F7 `DISK-BOOT-OK` → second Linux `root=UUID=` → `login:`. **ISO isolation proven. Leftover DRAM Everest ≠ persist.** USB LUN missed: p11 GET_DESC `cmd=4 cmpl=0`, p14 hub `1604:10c0` class 09 `err=4`. This EFI retries GET_DESC and skips hubs (`err=9`) so a later CCS BOT is not hidden. DESC retry EFI `6c278e85` / COM2 `6c278e859b89`: p11 Toshiba `0480:a004` GET_DESC succeeded (`usb I/O ready bytes=320072933376 lba=512`), leftover skipped, ISO `vdb` `last_st=0x0` `Mounting boot media: ok.` — **enum + ISO isolation proven.** Peek `read-fail usb_err=8 cmpl=0xff` at `off=0x200` (BOT data-stage Transfer Event timeout); guest `vda` IOERR; `sfdisk: cannot open /dev/vda: I/O error`; stall overlay looped `PIT paced n=6402`. INQUIRY/TUR/CAPACITY (≤36 B) are not a 512-byte READ. BOT READ EFI `73dc4d2e` / COM2 `73dc4d2e6eaa`: p11 Toshiba named, p14 `hub skip (err=9)`, then `usb I/O fail err=8 bot=csw cmpl=0 portsc=…0e03` (p14 hub stamped `cmpl=0` over the p11 CSW). **No** `usb I/O ready`. Leftover DRAM Everest completed. **Leftover DRAM Everest ≠ persist.** START STOP + ISP-on-every-IN + auto EP-reset regressed vs `6c278e85`. CSW EFI `68e16633` / COM2 `68e1663317a1`: p11 Toshiba named + p14 hub skip lived, then `usb I/O fail err=8 bot=cbw scsi=tur cmpl=0 portsc=0x0000000e00000e03`. Packed encoding is port **14** + PORTSC `0xe03`, not “LUN stuck on p10”. Scan-time p10/p11/p14 were all Polling `0x206e1` (HCRST already dropped PED). `scsi=tur` was the READ-probe recovery TUR stamped over the READ fail. Device `class=0x00 proto=0x00` is the device descriptor; BOT already parses config `08/06/50` + bulk EP addresses (`parse_bot_eps`). Full HCRST + own DCBAA/rings/scratch already runs after EBS (p11 GET_DESC on the same rings falsifies “cycle bit doesn’t match”). This EFI: always port-reset CCS (never inherit U0); hub skip never stamps `cmpl`/`portsc`; no TUR in bring-up or READ retry; COM2 prints `xhci hcrst` + `xhci bot pN iface=08/06/50 ep_out= ep_in=`; stamp BOT fail on the **current** port. Port EFI `96024edc` / COM2 `build: sha=96024edcf70f` (two boots, SET_CONFIG-first **not** flashed): (1) p11 Toshiba `0480:a004` + `xhci bot p11 iface=08/06/50 ep_out=2 ep_in=1 cfg=1` then `xhci enum p11 cmd=5 cmpl=0` (`err=8` SET_CONFIG after CONFIG_EP); fail `cmpl=0x0e801a40 bot=?` is p14 `reset_port` reset_diag. (2) Same SHA: **no** `cmd=5` line (SET_CONFIG+CONFIG_EP held) then `usb I/O fail err=8 portsc=…0e00000e03 cmpl=0 bot=cbw scsi=read` — CAPACITY (8 B IN) completed; native READ died on the CBW OUT. Packed `portsc` is still p14 on this unflashed EFI. Leftover DRAM Everest (`ISO-INSTALL-OK` → F7 `DISK-BOOT-OK` → `root=UUID=45213e85-…` → `login:`) is **not** persist. This EFI: SET_CONFIGURATION before Configure Endpoint (`xhci setcfg` / cmd `6`); later-port `reset_port` does not clobber Enum/Xfer; Transfer Event must match slot+DCI (`consume_transfer`; leftover CSW IN DCI 3 must not retire CBW OUT DCI 4); dedicated IN bounce; no Reset Endpoint on CBW fail; settle after CAPACITY. Need COM2 Toshiba `0480:a004` + `usb I/O ready` + virtio 298 GiB + `usb rw ok` + Alpine `vda` READs `last_st=0x0` (not leftover apk stall dump during ISO mount). `usb rw fail` must name `bot=`/`scsi=`. Do not setup-disk until virtio READ on the 298 GiB LUN works.

### 3. Phase B attach

SPA Start still needs HTTP. If PRE-EBS SNP DHCP failed (`SNP DHCP failed (no lease)`), this EFI still brings analog up after EBS and runs native BCM5720 DHCP at coexist. Expect `HOST-NIC BCM5720 DHCP discover` then `HOST-NIC BCM5720 DHCP lease` then `HOST-NIC coexist listening` / `CURL NOW`. Fail: `native DHCP failed` then `SPA Start needs HTTP` (same idle hang as `6ba076cc`).

Same Everest loop (`HOST-NIC-HTTP-OK` → SPA Start → install → F7 `DISK-BOOT-OK`), but virtio must be the LUN:

```
leftover install disk skip durable LUN
virtio-blk install disk bytes=… keep=0 (durable LUN nvme)
```

or `… (durable LUN usb)`.

**Not ready:** `leftover install disk hpa=… (leftover DRAM; durable LUN not ready)` then `virtio-blk … (leftover DRAM; durable LUN not ready)`. Do not Force Off expecting persist.

---

## Do not F11 until

1. A LUN the mapper will pick is seated.
2. COM2 shows `I/O ready` **and** `virtio-blk … (durable LUN nvme|usb)`.
3. Then one-time F11 this EFI (not `ce3d8a09` File-RAM). Ubuntu-on-PERC stays standing boot.

Force Off / reboot RayNu-V (not guest F7) is the iron close. Lived `4af78b43`: keep=1 + `DISK-BOOTX64` + `UUID=348005a9-…` after HV reboot. Minted `RAYNU-V-M8-DISK-PERSIST-OK` did not print. If the prototype misbehaves, re-flash [`v0.1.0-everest-closed`](https://github.com/vikkp/RayNu/releases/tag/v0.1.0-everest-closed).

## Not this

- Nested `MODE=full` / File RAM
- M8.1 TLS
- Formatting the PERC
- Printing `ISO-INSTALL-OK` from host/CI/nested
