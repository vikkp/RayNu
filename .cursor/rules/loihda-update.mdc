---
description: LOIHDA — update docs/loihda.md + site/loi.html tracker on every LOI-relevant step
globs:
  - "docs/loihda.md"
  - "docs/m8_plan.md"
  - "docs/runbooks/m8_persist_iron.md"
  - "site/loi.*"
  - "mgmt/durable_lun.rs"
  - "mgmt/disk_persist.rs"
  - "mgmt/usb_bot.rs"
  - "mgmt/xhci.rs"
alwaysApply: true
---

# LOIHDA auto-update (path to Letter of Intent)

You are working in **RayNu-V** (`vikkp/RayNu`). Everest HDA (`docs/hda.md`) measures the **closed** product loop. **LOIHDA** (`docs/loihda.md`) measures whether a **non-production LOI** is in scope, and which bar.

Public page: **`site/loi.html`** (reads **`site/loi.json`**). Sync: `./tools/sync-loihda-site.sh`.

Keep LOIHDA on the **working persist/TLS/PERC branch**, not only `cursor/loi-hda-page-8366`. Every iron USB/xHCI/BOT step is Bar A A2 work.

**Start at `docs/m8_state.md`** (known-good `928d6224`, prototype do-not-F11 table, diagnosis, Phase 0–3). One changelog row per iron boot; one scoreboard sentence per gate. Scores go **down** on regression evidence and **up** only on a repeated iron close.

## Two bars (do not mix)

| Bar | Meaning | Close |
|-----|---------|-------|
| **A dedicated-box** | One PowerEdge we own or they dedicate | Persist on COM2 + SKU card + TLS |
| **B RAID-fleet** | Replace the licensed HV on PERC VDs they already paid for | `RAYNU-V-M8-PERC-LUN-OK` on a **spare** VD, not Ubuntu |

HDA 99% ≠ LOI. USB persist ≠ PERC persist. Nested QEMU ≠ R640.

## When you MUST update LOIHDA

Same change (or tight follow-up) when you:

1. Commit / push / merge LOI-relevant work (persist, USB BOT/xHCI, PERC, TLS, auth, console, SKU honesty).
2. Close or open an M8 marker (`RAYNU-V-M8-DISK-PERSIST-OK`, `RAYNU-V-M8-PERC-LUN-OK`, TLS).
3. Decode iron COM2 on the persist path (pass **or** fail). A new `bot=` / `scsi=` / `cmpl=` is a LOIHDA changelog row even when scores **hold**.
4. User says: `loihda`, `update loi`, `path to LOI`.

Do **not** skip LOIHDA because HDA already moved. Do **not** bump Bar A / persist % without COM2 `RAYNU-V-M8-DISK-PERSIST-OK` (or a named piece close). Prefer under-claiming.

Also run the Everest HDA procedure if that rule also fires. The two trackers must not contradict (Everest stays closed; LOI stays open until Bar A is honest).

## Procedure

1. Read `docs/loihda.md` (frontmatter + scoreboard + changelog).
2. Re-score Bar A / Bar B pieces. Prefer under-claiming.
3. Set `months_to_loi_*_prev` before changing months. Only **decrease** months with COM2/DONE evidence.
4. Rewrite frontmatter, scoreboard, persist-piece status, this-commit delta, changelog.
5. `./tools/sync-loihda-site.sh` and `--check`. Stage `docs/loihda.md` + `site/loi.json`. If `site/loi.html` copy is stale, patch HTML in the same commit.
6. Footer: `LOIHDA: Bar A X%→Y% · Bar B A%→B% · months A M→N`

## Scope of claims

- RAID-fleet LOI waits on PERC virtual-disk persist. A USB demonstration is not that close.
- The Ubuntu PERC is not formatted as an LOI strategy.
- Host/CI/nested do not print iron persist or PERC markers.
- Everest remains closed. A patched Alpine ISO is a named residual, not a reopened summit.
- Leftover DRAM / `ISO-INSTALL-OK` is A1 noise, not A2 persist.
