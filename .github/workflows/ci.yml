name: ci

on:
  push:
    branches: [main, "cursor/**"]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  hda-site-sync:
    name: HDA site sync
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: site/hda.json matches docs/hda.md
        run: ./tools/sync-hda-site.sh --check

  test-host:
    name: host tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly
          components: rust-src, rustfmt, clippy
      - name: cargo test (host, no UEFI)
        run: cargo test --no-default-features

  build-uefi:
    name: UEFI release build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly
          components: rust-src
          targets: x86_64-unknown-uefi
      - name: build r640-hypervisor.efi
        run: ./tools/build.sh
      - name: size budget (ADR-003)
        run: ./tools/check-size.sh
      - name: package EFI release kit (M7.0)
        run: SKIP_BUILD=1 ./tools/package-release.sh
      - name: upload efi artifact
        uses: actions/upload-artifact@v4
        with:
          name: r640-hypervisor.efi
          path: target/x86_64-unknown-uefi/release/r640-hypervisor.efi
      - name: upload release kit
        uses: actions/upload-artifact@v4
        with:
          name: raynu-v-release-kit
          path: dist/

  # Boot gate: M0 → M3.8 Linux early / proto fallback (KVM nested when available).
  qemu-boot:
    name: QEMU boot gate (M0-M3.8)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly
          components: rust-src
          targets: x86_64-unknown-uefi
      - name: Install QEMU + OVMF
        run: |
          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq qemu-system-x86 ovmf
      - name: QEMU boot-to-serial gate
        run: |
          # Actions runners often have /dev/kvm root-only; open it for the job.
          if [[ -e /dev/kvm ]]; then
            sudo chmod a+rw /dev/kvm || true
            echo 1 | sudo tee /sys/module/kvm_intel/parameters/nested 2>/dev/null || true
            echo 1 | sudo tee /sys/module/kvm_amd/parameters/nested 2>/dev/null || true
          fi
          ls -l /dev/kvm 2>&1 || true
          echo -n "host cpu flags vmx/svm: "
          grep -E -o '\b(vmx|svm)\b' /proc/cpuinfo | sort -u | tr '\n' ' ' || true
          echo
          # Nested VT-x requires host CPUID.VMX; many cloud VMs expose KVM without it.
          if [[ -r /dev/kvm && -w /dev/kvm ]] && grep -qw vmx /proc/cpuinfo; then
            echo "KVM + host VMX — requiring VMXON through M3.8 early path"
            REQUIRE_VMX=1 QEMU_ACCEL=kvm ./tools/qemu-boot-test.sh
          elif [[ -r /dev/kvm && -w /dev/kvm ]]; then
            echo "KVM without host VMX — boot gate allows VMXON-SKIP"
            REQUIRE_VMX=0 QEMU_ACCEL=kvm ./tools/qemu-boot-test.sh
          else
            echo "KVM not usable — TCG boot gate allows VMXON-SKIP"
            REQUIRE_VMX=0 QEMU_ACCEL=tcg ./tools/qemu-boot-test.sh
          fi
      - name: Upload serial log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m0-serial-log
          path: |
            target/m0-serial.log
            target/m0-qemu-stdout.log
            target/m0-qemu-stderr.log
          if-no-files-found: ignore

  # M3.15: frozen Verus pin smoke (ADR-008). Hard-pass install + cargo verus.
  # Installs ONLY the exact tag/commit/sha256 in verus-version.toml — never "latest".
  # True L3 proof discharge remains M3.17 (crate does not opt into verify yet).
  verus-smoke:
    name: verus pin smoke (M3.15)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Host crate build uses the repo rust-toolchain.toml pin (not floating nightly).
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Assert frozen Verus pin (exact tag + commit + sha256)
        run: |
          set -euo pipefail
          pin=verus-version.toml
          test -f "$pin"
          grep -E '^version = "0\.[0-9]{4}\.' "$pin"
          grep -E '^tag = "release/' "$pin"
          grep -E '^commit = "[0-9a-f]{40}"' "$pin"
          grep -E '^sha256_linux = "[0-9a-f]{64}"' "$pin"
          grep -E '^toolchain = "' "$pin"
          if grep -E '^(tag|asset_linux|version) = ".*latest' "$pin"; then
            echo "error: pin must not reference latest" >&2
            exit 1
          fi
          if grep -E '^tag = ".*rolling' "$pin"; then
            echo "error: pin must not use rolling pre-release" >&2
            exit 1
          fi
          echo "Frozen pin OK:"
          grep -E '^(version|tag|commit|toolchain|asset_linux|sha256_linux) = ' "$pin"
      - name: Verus pin smoke → RAYNU-V-M3-VERUS-OK
        run: ./tools/verus-smoke.sh

  # M3.16: Verus-linkable EptMap (`ept_model`). Hard-pass under frozen pin.
  verus-link:
    name: verus L3-link (M3.16)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus L3-link smoke → RAYNU-V-M3-L3-LINK-OK
        run: ./tools/verus-link-smoke.sh

  # M3.17: true L3 — green verify, no admit(). Hard-fail.
  verus-verify:
    name: verus L3-verify (M3.17)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus L3-verify smoke → RAYNU-V-M3-L3-VERIFY-OK
        run: ./tools/verus-verify-smoke.sh

  # M3.18: ghost↔exec refinement. Hard-fail.
  verus-refine:
    name: verus L3-refine (M3.18)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus L3-refine smoke → RAYNU-V-M3-L3-REFINE-OK
        run: ./tools/verus-refine-smoke.sh

  # M4.6: N-guest exclusivity in ghost model (spec OK; ADR-006 claim is M4.7). Hard-fail.
  verus-nguest-spec:
    name: verus N-guest-spec (M4.6)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus N-guest-spec smoke → RAYNU-V-M4-NGUEST-SPEC-OK
        run: ./tools/verus-nguest-spec-smoke.sh

  # M4.7: true L3 N-guest exclusivity (ADR-006). Hard-fail.
  verus-nguest-verify:
    name: verus N-guest-verify (M4.7)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus N-guest-verify smoke → RAYNU-V-M4-NGUEST-VERIFY-OK
        run: ./tools/verus-nguest-verify-smoke.sh

  # M4.8: large-page (2M/1G) ghost *spec* (L3 discharge → M5). Hard-fail.
  verus-lpage-spec:
    name: verus large-page-spec (M4.8)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus large-page-spec smoke → RAYNU-V-M4-LPAGE-OK
        run: ./tools/verus-lpage-spec-smoke.sh

  # M5.7: large-page L3 discharge (ADR-004 / ADR-006). Hard-fail.
  verus-lpage-verify:
    name: verus large-page-verify (M5.7)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus large-page-verify smoke → RAYNU-V-M5-LPAGE-VERIFY-OK
        run: ./tools/verus-lpage-verify-smoke.sh

  # M5.8: NUMA in ghost *spec* (SRAT/SLIT; affinity L3 → M6). Hard-fail.
  verus-numa:
    name: verus NUMA-spec (M5.8)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus NUMA-spec smoke → RAYNU-V-M5-NUMA-OK
        run: ./tools/verus-numa-smoke.sh

  # M5.9: allocator↔EPT refine + scoped identity. Hard-fail.
  verus-alloc-refine:
    name: verus alloc-refine (M5.9)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus alloc-refine smoke → RAYNU-V-M5-ALLOC-REFINE-OK
        run: ./tools/verus-alloc-refine-smoke.sh

  # M6.0: EPT-violation exclusivity (ADR-004). Hard-fail.
  verus-eptvio:
    name: verus EPT-violation (M6.0)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus EPT-violation smoke → RAYNU-V-M6-EPTVIO-OK
        run: ./tools/verus-eptvio-smoke.sh

  # M6.1: HW PTE bit-decode correspondence. Hard-fail.
  verus-hwpte:
    name: verus HW PTE (M6.1)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus HW PTE smoke → RAYNU-V-M6-HWPTE-OK
        run: ./tools/verus-hwpte-smoke.sh

  # M6.2: NUMA affinity L3. Hard-fail.
  verus-numa-l3:
    name: verus NUMA affinity L3 (M6.2)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus NUMA affinity L3 smoke → RAYNU-V-M6-NUMA-L3-OK
        run: ./tools/verus-numa-l3-smoke.sh

  # M6.3: live migration page transfer exclusivity. Hard-fail.
  verus-migrate-xfer:
    name: verus migrate-xfer (M6.3)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus migrate-xfer smoke → RAYNU-V-M6-MIGRATE-XFER-OK
        run: ./tools/verus-migrate-xfer-smoke.sh

  # M4.9: N-guest ghost↔exec refine. Hard-fail.
  verus-nguest-refine:
    name: verus N-guest-refine (M4.9)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: Install unzip (Verus release zip)
        run: sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq unzip
      - name: Verus N-guest-refine smoke → RAYNU-V-M4-REFINE-OK
        run: ./tools/verus-nguest-refine-smoke.sh

  # M5.0: VM lifecycle API. Hard-fail.
  m5-life:
    name: M5.0 lifecycle (LIFE-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M5.0 lifecycle smoke → RAYNU-V-M5-LIFE-OK
        run: ./tools/m5-life-smoke.sh

  # M5.1: CLI + REST control plane. Hard-fail.
  m5-api:
    name: M5.1 API (API-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M5.1 API smoke → RAYNU-V-M5-API-OK
        run: ./tools/m5-api-smoke.sh

  # M6.4: REST auth (bring-up mock token). Hard-fail.
  m6-auth:
    name: M6.4 REST auth (AUTH-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M6.4 REST auth smoke → RAYNU-V-M6-AUTH-OK
        run: ./tools/m6-auth-smoke.sh

  # M6.5: deterministic PDF audit reports. Hard-fail.
  m6-pdf:
    name: M6.5 PDF reports (PDF-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M6.5 PDF smoke → RAYNU-V-M6-PDF-OK
        run: ./tools/m6-pdf-smoke.sh

  # M6.6: mock HA failover + security harden. Hard-fail.
  m6-ha:
    name: M6.6 HA harden (HA-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M6.6 HA smoke → RAYNU-V-M6-HA-OK
        run: ./tools/m6-ha-smoke.sh

  # M6.7: fault injection suite. Hard-fail.
  m6-fault:
    name: M6.7 fault injection (FAULT-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M6.7 fault smoke → RAYNU-V-M6-FAULT-OK
        run: ./tools/m6-fault-smoke.sh

  # M6.8: 72-hr soak thresholds (accelerated simulation). Hard-fail.
  m6-soak:
    name: M6.8 soak (SOAK-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M6.8 soak smoke → RAYNU-V-M6-SOAK-OK
        run: ./tools/m6-soak-smoke.sh

  # M6.9: external audit + spec review (pin + ept_model verify). Hard-fail.
  m6-ext:
    name: M6.9 external audit (EXT-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M6.9 external audit smoke → RAYNU-V-M6-EXT-OK
        run: ./tools/m6-ext-smoke.sh

  # M7.0: EFI release kit (scripts + runbook + host gate). Hard-fail.
  m7-ship:
    name: M7.0 EFI release kit (SHIP-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M7.0 ship smoke → RAYNU-V-M7-SHIP-OK
        run: ./tools/m7-ship-smoke.sh

  # M7.1: network HTTP mgmt (codec + host TCP). Hard-fail.
  m7-http:
    name: M7.1 network HTTP (HTTP-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M7.1 HTTP smoke → RAYNU-V-M7-HTTP-OK
        run: ./tools/m7-http-smoke.sh

  # M7.2: datastore / image library. Hard-fail.
  m7-store:
    name: M7.2 datastore (STORE-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M7.2 store smoke → RAYNU-V-M7-STORE-OK
        run: ./tools/m7-store-smoke.sh

  # M7.3: ISO deploy (extract-boot + virtio install disk). Hard-fail.
  m7-iso:
    name: M7.3 ISO deploy (ISO-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M7.3 ISO smoke → RAYNU-V-M7-ISO-OK
        run: ./tools/m7-iso-smoke.sh

  # M7.4: Ops Web UI create-VM + media. Hard-fail.
  m7-ui:
    name: M7.4 Ops UI (UI-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M7.4 UI smoke → RAYNU-V-M7-UI-OK
        run: ./tools/m7-ui-smoke.sh

  # M7.5: R640 boot scaffold (runbook + evidence). Hard-fail on scaffold only.
  # Does NOT claim RAYNU-V-R640-BOOT-OK (iron-only).
  m7-r640:
    name: M7.5 R640 scaffold (SCAFFOLD-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M7.5 R640 scaffold smoke → RAYNU-V-M7-R640-SCAFFOLD-OK
        run: ./tools/m7-r640-smoke.sh

  # Boot media maker (FAT img for iDRAC/USB). Hard-fail on layout smoke.
  boot-media:
    name: Boot media maker (BOOT-MEDIA-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dosfstools + mtools
        run: sudo apt-get update -qq && sudo apt-get install -y -qq dosfstools mtools
      - name: Boot-media smoke → RAYNU-V-M7-BOOT-MEDIA-OK
        run: ./tools/make-boot-media-smoke.sh

  # M5.2: embedded Web UI. Hard-fail.
  m5-webui:
    name: M5.2 Web UI (WEBUI-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M5.2 Web UI smoke → RAYNU-V-M5-WEBUI-OK
        run: ./tools/m5-webui-smoke.sh

  # M5.3: audit ring + hash chain. Hard-fail.
  m5-audit:
    name: M5.3 audit (AUDIT-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M5.3 audit smoke → RAYNU-V-M5-AUDIT-OK
        run: ./tools/m5-audit-smoke.sh

  # M5.4: SOX/ISO audit reports. Hard-fail.
  m5-report:
    name: M5.4 reports (REPORT-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M5.4 report smoke → RAYNU-V-M5-REPORT-OK
        run: ./tools/m5-report-smoke.sh

  # M5.5: VMware migrate (ADR-007). Hard-fail.
  m5-migrate:
    name: M5.5 migrate (MIGRATE-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M5.5 migrate smoke → RAYNU-V-M5-MIGRATE-OK
        run: ./tools/m5-migrate-smoke.sh

  # M5.6: Dell Tier-1 iDRAC health + topology (ADR-005). Hard-fail.
  m5-idrac:
    name: M5.6 iDRAC (IDRAC-OK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-01
          components: rust-src
      - name: M5.6 iDRAC smoke → RAYNU-V-M5-IDRAC-OK
        run: ./tools/m5-idrac-smoke.sh

  # M3.21: hard-fail Kani on M2.6 harnesses (lib-only; pinned in kani-version.toml).
  kani:
    name: kani (M3.21 hard-fail)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          components: rust-src
      - name: Kani smoke → RAYNU-V-M3-KANI-OK
        run: ./tools/kani-smoke.sh
