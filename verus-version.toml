# Verus pin per ADR-008.
# M3.14 landed an L3 *attempt* (lemmas + gaps in memory/ept_proof.rs) gated by
# host `cargo test` → RAYNU-V-M3-L3-OK. `cargo verus --verify` is still not a
# hard CI gate — Verus remains unpinned. Pin an exact release here before
# claiming ADR-006 L3 maturity for EptMap.

[verus]
# Placeholder: replace with an exact git tag / release when Verus is installed in CI.
version = "unpinned-scaffold"
# Example future pin:
# version = "0.2025.12.01"
# git = "https://github.com/verus-lang/verus"
# rev = "REPLACE_ME"

[notes]
status = "M3.14 L3-attempt for ADR-004 (4K single-guest); Verus optional / soft-fail until pin + green verify"
l3_attempt = "memory/ept_proof.rs"
host_marker = "RAYNU-V-M3-L3-OK"
